---
layout: post
title: 숨바꼭질 
# subtitle: 부제목
# description: >
#   설명
date: '2022-11-13 22:0:0'
categories:
  - problem-solving
  - problem
tags: [boj,bfs,dp,java]
related_posts:
  - 
sitemap: true
published: true
---
# 숨바꼭질  🌻🌼

* toc 
{:toc}

- 🌻🌼
- 🟢

## 문제

<https://www.acmicpc.net/problem/1697>{:target="_blank"}

|시간 제한|    메모리 제한|    제출|    정답|    맞힌 사람|    정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|2 초|256 MB|27901|16502|12429|%|

**문제**  
수빈이는 동생과 숨바꼭질을 하고 있다. 수빈이는 현재 점 N(0 ≤ N ≤ 100,000)에 있고, 동생은 점 K(0 ≤ K ≤ 100,000)에 있다. 수빈이는 걷거나 순간이동을 할 수 있다. 만약, 수빈이의 위치가 X일 때 걷는다면 1초 후에 X-1 또는 X+1로 이동하게 된다. 순간이동을 하는 경우에는 1초 후에 2*X의 위치로 이동하게 된다.

수빈이와 동생의 위치가 주어졌을 때, 수빈이가 동생을 찾을 수 있는 가장 빠른 시간이 몇 초 후인지 구하는 프로그램을 작성하시오.

**입력**  
첫 번째 줄에 수빈이가 있는 위치 N과 동생이 있는 위치 K가 주어진다. N과 K는 정수이다.

**출력**  
수빈이가 동생을 찾는 가장 빠른 시간을 출력한다.

**예제 입력**

```
5 17
```

**예제 출력**

```
4
```

**힌트**  
수빈이가 5-10-9-18-17 순으로 가면 4초만에 동생을 찾을 수 있다.

## 나의 풀이

### bfs

- n,k의 범위가 0 이상 100000 이하이므로 그 범위를 담을 수 있는 배열을 만들고 거기에 시간을 기록하는식으로 풀었다
- 사실 이 범위를 다 확인할 필요가 없는데 굉장히 대충 푼 느낌이긴 하다

```java
import java.util.LinkedList;
import java.util.Queue;

public class Solution {
    static final int LIMIT = 100_001;
    int[] visited = new int[LIMIT];


    public int solution(int n, int k) {

        if (n == k) {
            return 0;
        }

        Queue<Integer> q = new LinkedList<>();
        q.offer(n);
        visited[n] = 1;
        while (!q.isEmpty()) {
            int cur = q.poll();

            if (cur == k) {
                break;
            }
            int next = 0;
            for (int i = 0; i < 3; i++) {
                if (i == 0) {
                    next = cur + 1;
                } else if (i == 1) {
                    next = cur - 1;
                } else {
                    next = cur * 2;
                }

                if (next < 0 || next >= LIMIT) {
                    continue;
                }

                if (visited[next] != 0) {
                    continue;
                }

                visited[next] = visited[cur] + 1;
                q.offer(next);
            }
        }
        return visited[k] - 1;
    }
}
```

```java
class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(4, s.solution(5, 17));
    }
}
```

### 재귀
- 다른 사람의 코드를 보니 재귀로 깔끔하게 풀었길래 참고했다
- n이 k보다 큰 경우는 무조건 +1 해서 가는 방법밖에 없으므로 바로 n-k 리턴
- 이제 n보다 k가 큰 경우만 고려하면 되는데, k가 1인 경우는 n이 0이니까 바로 1 리턴
- k가 홀수/짝수인 경우 나누어 생각
  - 홀수 : k+1, k-1인 경우를 계산한 것 중 더 작은 값에 +1 리턴
  - 짝수 : k/2인 경우 계산한 것에 +1한 값과 그냥 k-n (+1로만 가는 경우) 값 중 더 작은 것 리턴

```java
public class Solution {
    int n;

    public int solution(int n, int k) {
        this.n = n;
        return recur(k);
    }

    private int recur(int k) {
        if (n >= k) {
            return n - k;
        }
        if (k == 1) {
            return 1;
        }
        if (k % 2 == 1) {
            return Math.min(recur(k + 1), recur(k - 1)) + 1;
        }
        return Math.min(recur(k / 2) + 1, k - n);
    }
}
```

### dp

- 처음에 문제를 본 순간 dp 문제 느낌이 났지만 일단 bfs 카테고리에 있어서 bfs로 먼저 풀고 dp로도 풀어봤다
- n==k, n>k인 경우 먼저 처리
- n-1~0까지 차례로 증가시켜줘야 함
- 헷갈리는 부분은 `dp[(i-1)/2]+1`는 안되고 `dp[(i+1)/2]+1`만 통과되는 점..

```java
public class Solution {
    public int solution(int n, int k) {
        if (n == k) {
            return 0;
        }
        if (n > k) {
            return n - k;
        }

        int[] dp = new int[k + 1];

        for (int i = n - 1; i >= 0; i--) {
            dp[i] = dp[i + 1] + 1;
        }

        for (int i = n + 1; i < k + 1; i++) {
            if (i % 2 == 0) {
                dp[i] = Math.min(dp[i - 1], dp[i / 2]) + 1;
            } else {
                dp[i] = Math.min(dp[i - 1], dp[(i + 1) / 2] + 1) + 1;
            }
        }
        return dp[k];
    }
}
```

## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        int n = Integer.parseInt(st.nextToken());
        int k = Integer.parseInt(st.nextToken());
        System.out.println(solution(n, k));
    }

    private static int solution(int n, int k) {
        if (n == k) {
            return 0;
        }
        if (n > k) {
            return n - k;
        }

        int[] dp = new int[k + 1];

        for (int i = n - 1; i >= 0; i--) {
            dp[i] = dp[i + 1] + 1;
        }

        for (int i = n + 1; i < k + 1; i++) {
            if (i % 2 == 0) {
                dp[i] = Math.min(dp[i - 1], dp[i / 2]) + 1;
            } else {
                dp[i] = Math.min(dp[i - 1], dp[(i + 1) / 2] + 1) + 1;
            }
        }
        return dp[k];
    }
}
```

![image](https://user-images.githubusercontent.com/90626691/201530655-1e979135-e3ee-4f98-b823-2438ff34b62c.png)
