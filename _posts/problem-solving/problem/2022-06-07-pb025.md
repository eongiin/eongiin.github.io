---
layout: post
title: 특정한 최단 경로
# subtitle: 부제목
# description: >
#   설명
date: '2022-06-07 10:0:0'
categories:
  - problem-solving
  - problem
tags: [boj,dijkstra,essential,graph,java]
related_posts:
  - 
sitemap: true
published: true
---
# 특정한 최단 경로 🌻🌻

* toc 
{:toc}

- 🌻🌻
- 🟢

## 문제

<https://www.acmicpc.net/problem/1504>{:target="_blank"}

|시간 제한|	메모리 제한|	제출|	정답|	맞힌 사람|	정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|1 초|256 MB|50323|12448|8362|24.508%|

**문제**  
방향성이 없는 그래프가 주어진다. 세준이는 1번 정점에서 N번 정점으로 최단 거리로 이동하려고 한다. 또한 세준이는 두 가지 조건을 만족하면서 이동하는 특정한 최단 경로를 구하고 싶은데, 그것은 바로 임의로 주어진 두 정점은 반드시 통과해야 한다는 것이다.

세준이는 한번 이동했던 정점은 물론, 한번 이동했던 간선도 다시 이동할 수 있다. 하지만 반드시 최단 경로로 이동해야 한다는 사실에 주의하라. 1번 정점에서 N번 정점으로 이동할 때, 주어진 두 정점을 반드시 거치면서 최단 경로로 이동하는 프로그램을 작성하시오.

**입력**  
첫째 줄에 정점의 개수 N과 간선의 개수 E가 주어진다. (2 ≤ N ≤ 800, 0 ≤ E ≤ 200,000) 둘째 줄부터 E개의 줄에 걸쳐서 세 개의 정수 a, b, c가 주어지는데, a번 정점에서 b번 정점까지 양방향 길이 존재하며, 그 거리가 c라는 뜻이다. (1 ≤ c ≤ 1,000) 다음 줄에는 반드시 거쳐야 하는 두 개의 서로 다른 정점 번호 v1과 v2가 주어진다. (v1 ≠ v2, v1 ≠ N, v2 ≠ 1) 임의의 두 정점 u와 v사이에는 간선이 최대 1개 존재한다.

**출력**  
첫째 줄에 두 개의 정점을 지나는 최단 경로의 길이를 출력한다. 그러한 경로가 없을 때에는 -1을 출력한다.

**예제 입력**

```
4 6
1 2 3
2 3 3
3 4 1
1 3 5
2 4 5
1 4 4
2 3
```

**예제 출력**

```
7
```

## 나의 풀이

- 두 가지 방법 중 더 짧은 것 구하면 됨
    - Start → A → B → End
    - Start → B → A → End
    - `start에서 a로 가는 최단 경로 + a에서 b로 가는 최단 경로 + b에서 end로 가는 최단 경로`  
    위와 같은 방법으로 각 경우를 구할 수 있다

```java

import java.util.ArrayList;
import java.util.PriorityQueue;

public class Solution {

    static final int INF = 20_000_000;
    ArrayList<Node>[] graph;

    public int solution(int n, int e, int[][] arr, int via1, int via2) {
        graph = new ArrayList[n + 1];

        for (int i = 1; i < n + 1; i++) {
            graph[i] = new ArrayList<>();
        }

        for (int i = 0; i < e; i++) {
            graph[arr[i][0]].add(new Node(arr[i][1], arr[i][2]));
            graph[arr[i][1]].add(new Node(arr[i][0], arr[i][2]));
        }

        int case1 = 0;
        int case2 = 0;

        case1 += dijkstra(n, 1, via1);
        case1 += dijkstra(n, via1, via2);
        case1 += dijkstra(n, via2, n);

        case2 += dijkstra(n, 1, via2);
        case2 += dijkstra(n, via2, via1);
        case2 += dijkstra(n, via1, n);

        if (case1 >= INF && case2 >= INF) {
            return -1;
        }

        return Math.min(case1, case2);
    }

    int dijkstra(int n, int start, int end) {

        int[] dist = new int[n + 1];
        for (int i = 1; i < n + 1; i++) {
            dist[i] = INF;
        }

        boolean[] visited = new boolean[n + 1];

        PriorityQueue<Node> pq = new PriorityQueue<>();

        pq.offer(new Node(start, 0));
        dist[start] = 0;

        while (!pq.isEmpty()) {
            Node cur = pq.poll();

            if (visited[cur.num]) {
                continue;
            }
            visited[cur.num] = true;

            if (dist[cur.num] < cur.cost) {
                continue;
            }

            for (Node adj : graph[cur.num]) {
                int newCost = cur.cost + adj.cost;
                if (!visited[adj.num] && dist[adj.num] > newCost) {
                    dist[adj.num] = newCost;
                    pq.offer(new Node(adj.num, newCost));
                }
            }
        }

        return dist[end];
    }

    class Node implements Comparable<Node> {
        int num;
        int cost;

        public Node(int num, int cost) {
            this.num = num;
            this.cost = cost;
        }

        @Override
        public int compareTo(Node o) {
            return this.cost - o.cost;
        }
    }
}
```

```java
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(7, s.solution(4, 6, new int[][]{
                {1, 2, 3},
                {2, 3, 3},
                {3, 4, 1},
                {1, 3, 5},
                {2, 4, 5},
                {1, 4, 4},
        }, 2, 3));
    }
}
```

## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.*;


public class Main {

    static final int INF = 20_000_000;
    static ArrayList<Node>[] graph;

    public static void main(String[] args) throws IOException {

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());

        int n = Integer.parseInt(st.nextToken());
        int e = Integer.parseInt(st.nextToken());

        graph = new ArrayList[n + 1];
        for (int i = 1; i < n + 1; i++) {
            graph[i] = new ArrayList<>();
        }
        for (int i = 0; i < e; i++) {
            st = new StringTokenizer(br.readLine());

            int a = Integer.parseInt(st.nextToken());
            int b = Integer.parseInt(st.nextToken());
            int cost = Integer.parseInt(st.nextToken());

            graph[a].add(new Node(b, cost));
            graph[b].add(new Node(a, cost));
        }

        st = new StringTokenizer(br.readLine());
        int via1 = Integer.parseInt(st.nextToken());
        int via2 = Integer.parseInt(st.nextToken());

        int case1 = 0;
        int case2 = 0;

        case1 += dijkstra(n, 1, via1);
        case1 += dijkstra(n, via1, via2);
        case1 += dijkstra(n, via2, n);

        case2 += dijkstra(n, 1, via2);
        case2 += dijkstra(n, via2, via1);
        case2 += dijkstra(n, via1, n);

        if (case1 >= INF && case2 >= INF) {
            System.out.println(-1);
        } else {
            System.out.println(Math.min(case1, case2));
        }

    }

    static int dijkstra(int n, int start, int end) {

        int[] dist = new int[n + 1];
        for (int i = 1; i < n + 1; i++) {
            dist[i] = INF;
        }
        boolean[] visited = new boolean[n + 1];
        PriorityQueue<Node> pq = new PriorityQueue<>();

        pq.offer(new Node(start, 0));
        dist[start] = 0;

        while (!pq.isEmpty()) {
            Node cur = pq.poll();

            if (visited[cur.num]) {
                continue;
            }
            visited[cur.num] = true;

            if (dist[cur.num] < cur.cost) {
                continue;
            }

            for (Node adj : graph[cur.num]) {
                int newCost = cur.cost + adj.cost;
                if (!visited[adj.num] && dist[adj.num] > newCost) {
                    dist[adj.num] = newCost;
                    pq.offer(new Node(adj.num, newCost));
                }
            }
        }

        return dist[end];
    }

    static class Node implements Comparable<Node> {
        int num;
        int cost;

        public Node(int num, int cost) {
            this.num = num;
            this.cost = cost;
        }

        @Override
        public int compareTo(Node o) {
            return this.cost - o.cost;
        }
    }
}
```

|메모리|시간|
|:---:|:---:|
|68840KB|704ms|