---
layout: post
title: 제목
# subtitle: 부제목
# description: >
#   설명
date: '2023-02-11 22:0:0'
categories:
  - problem-solving
  - problem
tags: [boj,dp,recursive,java]
related_posts:
  - 
sitemap: true
published: true
---
# [Gold4] 팰린드롬? 🌻🌼

* toc 
{:toc}

- 🌻🌼
- 🟢

## 문제

<https://www.acmicpc.net/problem/10942>{:target="_blank"}

|시간 제한|    메모리 제한|    제출|    정답|    맞힌 사람|    정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|2.5 초|256 MB|43681|12499|8575|29.488%|

**문제**  
명우는 홍준이와 함께 팰린드롬 놀이를 해보려고 한다.

먼저, 홍준이는 자연수 N개를 칠판에 적는다. 그 다음, 명우에게 질문을 총 M번 한다.

각 질문은 두 정수 S와 E(1 ≤ S ≤ E ≤ N)로 나타낼 수 있으며, S번째 수부터 E번째 까지 수가 팰린드롬을 이루는지를 물어보며, 명우는 각 질문에 대해 팰린드롬이다 또는 아니다를 말해야 한다.

예를 들어, 홍준이가 칠판에 적은 수가 1, 2, 1, 3, 1, 2, 1라고 하자.

- S = 1, E = 3인 경우 1, 2, 1은 팰린드롬이다.
- S = 2, E = 5인 경우 2, 1, 3, 1은 팰린드롬이 아니다.
- S = 3, E = 3인 경우 1은 팰린드롬이다.
- S = 5, E = 7인 경우 1, 2, 1은 팰린드롬이다.
자연수 N개와 질문 M개가 모두 주어졌을 때, 명우의 대답을 구하는 프로그램을 작성하시오.

**입력**  
첫째 줄에 수열의 크기 N (1 ≤ N ≤ 2,000)이 주어진다.

둘째 줄에는 홍준이가 칠판에 적은 수 N개가 순서대로 주어진다. 칠판에 적은 수는 100,000보다 작거나 같은 자연수이다.

셋째 줄에는 홍준이가 한 질문의 개수 M (1 ≤ M ≤ 1,000,000)이 주어진다.

넷째 줄부터 M개의 줄에는 홍준이가 명우에게 한 질문 S와 E가 한 줄에 하나씩 주어진다.

**출력**  
총 M개의 줄에 걸쳐 홍준이의 질문에 대한 명우의 답을 입력으로 주어진 순서에 따라서 출력한다. 팰린드롬인 경우에는 1, 아닌 경우에는 0을 출력한다.

**예제 입력**

```
7
1 2 1 3 1 2 1
4
1 3
2 5
3 3
5 7
```

**예제 출력**

```
1
0
1
1
```

## 나의 풀이
- 수열의 크기 N이 2000까지인데, 그걸 최대 1,000,000 번을 매번 확인하면 시간 초과가 발생하므로 확인한 값들은 저장을 하고 활용을 해야 함

### 재귀

- 이차원 배열 memo에 확인한 값들을 저장해서 활용할 예정
- memo[i][j]는 배열의 i부터 j까지 팰린드롬이 맞는지 확인한 값 저장
    - 확인하지 않은 경우는 -1, 확인한 경우는 1(참인 경우) 혹은 0(거짓인 경우)
- 확인하려는 구간 [i,j]가 이미 전에 확인한 곳이라면 다시 계산하지 않고 memo[i][j] 값 활용
- 맨 끝 값이 서로 같은지 비교하고 가운데 값은 재귀로 구하는데, 계속 범위를 좁히다가 마지막 left, right 값이 같거나 1만 차이 나면 바로 값 반환

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Arrays;
import java.util.StringTokenizer;

public class Main {

    static int[] numbers;
    static int[][] memo;

    public static void main(String[] args) throws IOException {

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        numbers = new int[n];
        StringTokenizer st = new StringTokenizer(br.readLine());
        for (int i = 0; i < n; i++) {
            numbers[i] = Integer.parseInt(st.nextToken());
        }

        memo = new int[n][n];
        for (int i = 0; i < n; i++) {
            Arrays.fill(memo[i], -1);
        }

        int m = Integer.parseInt(br.readLine());
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < m; i++) {
            st = new StringTokenizer(br.readLine());
            int start = Integer.parseInt(st.nextToken()) - 1;
            int end = Integer.parseInt(st.nextToken()) - 1;
            sb.append(isPalindrome(start, end)).append("\n");
        }
        System.out.println(sb);
    }

    static int isPalindrome(int left, int right) {
        if (memo[left][right] != -1) {
            return memo[left][right];
        }
        if (left == right) {
            return memo[left][right] = 1;
        }
        if (right - left == 1) {
            if (numbers[left] == numbers[right]) {
                return memo[left][right] = 1;
            } else {
                return memo[left][right] = 0;
            }
        }

        if ((numbers[left] == numbers[right]) && isPalindrome(left + 1, right - 1) == 1) {
            return memo[left][right] = 1;
        } else {
            return memo[left][right] = 0;
        }
    }
}
```
### 반복문

- dp[i][j] : j-i+1 ~ j까지 i 개의 숫자들이 팰린드롬인지 참 거짓 저장
    - i : 확인할 숫자의 개수
    - j : 확인하는 마지막 숫자의 위치(가장 오른쪽에 있는 수)
- dp[1][j]는 숫자가 하나뿐이니까 모두 참
- dp[2][j]는 두 개의 숫잔데, 두 수가 같으면 참 아니면 거짓
- dp[3][j]부터 `양쪽 끝 값이 같고 가운데 숫자들이 참이면` 참
    - 양쪽 끝이 같은지는 `numbers[j-i+1] == numbers[j]`로 확인
    - 가운데 부분이 같은지는 `dp[i-2][j-1]`로 확인
        - 가운데 부분은 양쪽 끝을 제외한 부분이니까 i-2개의 숫자가 있을 것이고 j-1에서 끝나므로 dp[i-2][j-1]로 알 수 있는 것

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Arrays;
import java.util.StringTokenizer;

public class Main {

    public static void main(String[] args) throws IOException {

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int[] numbers = new int[n + 1];
        StringTokenizer st = new StringTokenizer(br.readLine());
        for (int i = 1; i <= n; i++) {
            numbers[i] = Integer.parseInt(st.nextToken());
        }

        boolean[][] dp = new boolean[n + 1][n + 1];
        Arrays.fill(dp[1], true);

        for (int i = 2; i <= n; i++) {
            if (numbers[i - 1] == numbers[i]) {
                dp[2][i] = true;
            }
        }

        for (int i = 3; i <= n; i++) {
            for (int j = i; j <= n; j++) {
                if (dp[i - 2][j - 1] && numbers[j - i + 1] == numbers[j]) {
                    dp[i][j] = true;
                }
            }
        }

        int m = Integer.parseInt(br.readLine());
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < m; i++) {
            st = new StringTokenizer(br.readLine());
            int start = Integer.parseInt(st.nextToken());
            int end = Integer.parseInt(st.nextToken());
            sb.append(dp[end - start + 1][end] ? 1 : 0).append("\n");
        }
        System.out.println(sb);
    }
}
```

|메모리|시간|
|:---:|:---:|
|220756 KB|844 ms|