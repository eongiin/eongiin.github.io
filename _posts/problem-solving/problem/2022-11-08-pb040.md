---
layout: post
title: 안전 영역
# subtitle: 부제목
# description: >
#   설명
date: '2022-11-08 16:40:0'
categories:
  - problem-solving
  - problem
tags: [boj,bfs,brute-force,dfs,essential,graph,java]
related_posts:
  - 
sitemap: true
published: true
---
# 안전 영역 🌻🌼

* toc 
{:toc}

- 🌻🌼
- 🟢

## 문제

<https://www.acmicpc.net/problem/2468>{:target="_blank"}

|시간 제한|    메모리 제한|    제출|    정답|    맞힌 사람|    정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|1 초|128  MB|70151|26283|17625|34.109%|

**문제**  
재난방재청에서는 많은 비가 내리는 장마철에 대비해서 다음과 같은 일을 계획하고 있다. 먼저 어떤 지역의 높이 정보를 파악한다. 그 다음에 그 지역에 많은 비가 내렸을 때 물에 잠기지 않는 안전한 영역이 최대로 몇 개가 만들어 지는 지를 조사하려고 한다. 이때, 문제를 간단하게 하기 위하여, 장마철에 내리는 비의 양에 따라 일정한 높이 이하의 모든 지점은 물에 잠긴다고 가정한다.

어떤 지역의 높이 정보는 행과 열의 크기가 각각 N인 2차원 배열 형태로 주어지며 배열의 각 원소는 해당 지점의 높이를 표시하는 자연수이다. 예를 들어, 다음은 N=5인 지역의 높이 정보이다.

![image](https://user-images.githubusercontent.com/90626691/200459776-6046b870-1f0e-4452-9cbe-7dbf37ce82cf.png){: .align-center}

이제 위와 같은 지역에 많은 비가 내려서 높이가 4 이하인 모든 지점이 물에 잠겼다고 하자. 이 경우에 물에 잠기는 지점을 회색으로 표시하면 다음과 같다. 

![image](https://user-images.githubusercontent.com/90626691/200503375-8700a32f-3776-4e04-967e-7f66e3fad223.png){: .align-center}

물에 잠기지 않는 안전한 영역이라 함은 물에 잠기지 않는 지점들이 위, 아래, 오른쪽 혹은 왼쪽으로 인접해 있으며 그 크기가 최대인 영역을 말한다. 위의 경우에서 물에 잠기지 않는 안전한 영역은 5개가 된다(꼭짓점으로만 붙어 있는 두 지점은 인접하지 않는다고 취급한다). 

또한 위와 같은 지역에서 높이가 6이하인 지점을 모두 잠기게 만드는 많은 비가 내리면 물에 잠기지 않는 안전한 영역은 아래 그림에서와 같이 네 개가 됨을 확인할 수 있다. 

![image](https://user-images.githubusercontent.com/90626691/200503781-19297ef4-b5f2-4c52-aed4-8acc83c009e8.png){: .align-center}

이와 같이 장마철에 내리는 비의 양에 따라서 물에 잠기지 않는 안전한 영역의 개수는 다르게 된다. 위의 예와 같은 지역에서 내리는 비의 양에 따른 모든 경우를 다 조사해 보면 물에 잠기지 않는 안전한 영역의 개수 중에서 최대인 경우는 5임을 알 수 있다. 

어떤 지역의 높이 정보가 주어졌을 때, 장마철에 물에 잠기지 않는 안전한 영역의 최대 개수를 계산하는 프로그램을 작성하시오. 

(아무 지역도 물에 잠기지 않을 수도 있다.)

**입력**  
첫째 줄에는 어떤 지역을 나타내는 2차원 배열의 행과 열의 개수를 나타내는 수 N이 입력된다. N은 2 이상 100 이하의 정수이다. 둘째 줄부터 N개의 각 줄에는 2차원 배열의 첫 번째 행부터 N번째 행까지 순서대로 한 행씩 높이 정보가 입력된다. 각 줄에는 각 행의 첫 번째 열부터 N번째 열까지 N개의 높이 정보를 나타내는 자연수가 빈 칸을 사이에 두고 입력된다. 높이는 1이상 100 이하의 정수이다.

**출력**  
첫째 줄에 장마철에 물에 잠기지 않는 안전한 영역의 최대 개수를 출력한다.

**예제 입력1**

```
5
6 8 2 6 2
3 2 3 4 6
6 7 3 3 2
7 2 5 3 6
8 9 5 2 7
```

**예제 출력1**

```
5
```

**예제 입력2**

```
7
9 9 9 9 9 9 9
9 2 1 2 1 2 9
9 1 8 7 8 1 9
9 2 7 9 7 2 9
9 1 8 7 8 1 9
9 2 1 2 1 2 9
9 9 9 9 9 9 9
```

**예제 출력2**

```
6
```


## 나의 풀이

- 높이는 1부터 100까지 가능한데 어차피 주어진 배열 최솟값 미만과 최댓값 초과인 범위는 결과가 같을 것이므로 최솟값과 최댓값을 구해서 그 범위에서 하나씩 확인
- DFS, BFS 둘 다 풀 수 있으나 이 문제는 DFS로 푸는 것이 메모리가 훨씬 절약되고 빠르다
- 기존 DFS, BFS에 브루트 포스만 추가된 문제

### DFS
```java
public class Solution {

    static int n;
    static int[][] arr;
    static boolean[][] visited;
    static final int[][] dirs = { {-1, 0}, {1, 0}, {0, -1}, {0, 1} };

    public int solution(int n, int[][] arr) {

        this.n = n;
        this.arr = arr;


        int start = 100;
        int end = 1;

        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                int cur = arr[i][j];
                start = Math.min(start, cur);
                end = Math.max(end, cur);
            }
        }

        int max = 1;

        for (int h = start; h <= end; h++) {
            visited = new boolean[n][n];
            int count = 0;
            for (int i = 0; i < n; i++) {
                for (int j = 0; j < n; j++) {
                    if (!visited[i][j] && arr[i][j] > h) {
                        dfs(h, i, j);
                        count++;
                    }
                }
            }
            max = Math.max(max, count);
        }
        return max;
    }

    private void dfs(int height, int r, int c) {
        visited[r][c] = true;
        for (int[] dir : dirs) {
            int nr = r + dir[0];
            int nc = c + dir[1];
            if (nr >= 0 && nr < n && nc >= 0 && nc < n) {
                if (!visited[nr][nc] && arr[nr][nc] > height) {
                    dfs(height, nr, nc);
                }
            }
        }
    }
}
```

```java
class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(5, s.solution(5, new int[][]{
                {6, 8, 2, 6, 2},
                {3, 2, 3, 4, 6},
                {6, 7, 3, 3, 2},
                {7, 2, 5, 3, 6},
                {8, 9, 5, 2, 7}
        }));

        Assertions.assertEquals(6, s.solution(7, new int[][]{
                {9, 9, 9, 9, 9, 9, 9},
                {9, 2, 1, 2, 1, 2, 9},
                {9, 1, 8, 7, 8, 1, 9},
                {9, 2, 7, 9, 7, 2, 9},
                {9, 1, 8, 7, 8, 1, 9},
                {9, 2, 1, 2, 1, 2, 9},
                {9, 9, 9, 9, 9, 9, 9}
        }));
    }
}
```

### BFS

Queue에 넣을 때 주의 사항

- visited 체크를 먼저 해주고 넣어야 한다
- 일단 넣고 나중에 체크하면 메모리 초과 발생

```java
import java.util.LinkedList;
import java.util.Queue;

public class Solution {

    static int n;
    static int[][] arr;
    static boolean[][] visited;
    static final int[][] dirs = { {-1, 0}, {1, 0}, {0, -1}, {0, 1} };

    public int solution(int n, int[][] arr) {

        this.n = n;
        this.arr = arr;


        int start = 100;
        int end = 1;

        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                int cur = arr[i][j];
                start = Math.min(start, cur);
                end = Math.max(end, cur);
            }
        }

        int max = 1;

        for (int h = start; h <= end; h++) {
            visited = new boolean[n][n];
            int count = 0;
            for (int i = 0; i < n; i++) {
                for (int j = 0; j < n; j++) {
                    if (!visited[i][j] && arr[i][j] > h) {
                        bfs(h, i, j);
                        count++;
                    }
                }
            }
            max = Math.max(max, count);
        }
        return max;
    }

    private void bfs(int height, int r, int c) {
        Queue<int[]> q = new LinkedList<>();
        q.offer(new int[]{r, c});
        visited[r][c] = true;

        while (!q.isEmpty()) {
            int[] cur = q.poll();
            int cr = cur[0];
            int cc = cur[1];

            for (int[] dir : dirs) {
                int nr = cr + dir[0];
                int nc = cc + dir[1];
                if (nr >= 0 && nr < n && nc >= 0 && nc < n) {
                    if (!visited[nr][nc] && arr[nr][nc] > height) {
                        visited[nr][nc] = true;
                        q.offer(new int[]{nr, nc});
                    }
                }
            }
        }
    }
}
```

## 최종 코드

### DFS

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Main {
    static int n;
    static int[][] arr;
    static boolean[][] visited;
    static final int[][] dirs = { {-1, 0}, {1, 0}, {0, -1}, {0, 1} };

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        n = Integer.parseInt(st.nextToken());
        arr = new int[n][n];

        int start = 100;
        int end = 1;
        for (int i = 0; i < n; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < n; j++) {
                int cur = Integer.parseInt(st.nextToken());
                arr[i][j] = cur;
                start = Math.min(start, cur);
                end = Math.max(end, cur);
            }
        }

        System.out.println(solution(start, end));
    }


    private static int solution(int start, int end) {
        int max = start != 1 ? 1 : 0;

        for (int h = start; h <= end; h++) {
            visited = new boolean[n][n];
            int count = 0;
            for (int i = 0; i < n; i++) {
                for (int j = 0; j < n; j++) {
                    if (!visited[i][j] && arr[i][j] > h) {
                        dfs(h, i, j);
                        count++;
                    }
                }
            }
            max = Math.max(max, count);
        }
        return max;
    }

    private static void dfs(int height, int r, int c) {
        visited[r][c] = true;
        for (int[] dir : dirs) {
            int nr = r + dir[0];
            int nc = c + dir[1];
            if (nr >= 0 && nr < n && nc >= 0 && nc < n) {
                if (!visited[nr][nc] && arr[nr][nc] > height) {
                    dfs(height, nr, nc);
                }
            }
        }
    }
}
```

|메모리|시간|
|:---:|:---:|
|18032 KB|224 ms|


### BFS

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.LinkedList;
import java.util.Queue;
import java.util.StringTokenizer;

public class Main {
    static int n;
    static int[][] arr;
    static boolean[][] visited;
    static final int[][] dirs = { {-1, 0}, {1, 0}, {0, -1}, {0, 1} };

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        n = Integer.parseInt(st.nextToken());
        arr = new int[n][n];

        int start = 100;
        int end = 1;
        for (int i = 0; i < n; i++) {
            st = new StringTokenizer(br.readLine());
            for (int j = 0; j < n; j++) {
                int cur = Integer.parseInt(st.nextToken());
                arr[i][j] = cur;
                start = Math.min(start, cur);
                end = Math.max(end, cur);
            }
        }

        System.out.println(solution(start, end));
    }


    private static int solution(int start, int end) {
        int max = 1;

        for (int h = start; h <= end; h++) {
            visited = new boolean[n][n];
            int count = 0;
            for (int i = 0; i < n; i++) {
                for (int j = 0; j < n; j++) {
                    if (!visited[i][j] && arr[i][j] > h) {
                        bfs(h, i, j);
                        count++;
                    }
                }
            }
            max = Math.max(max, count);
        }
        return max;
    }

    private static void bfs(int height, int r, int c) {
        Queue<int[]> q = new LinkedList<>();
        q.offer(new int[]{r, c});
        visited[r][c] = true;

        while (!q.isEmpty()) {
            int[] cur = q.poll();
            int cr = cur[0];
            int cc = cur[1];

            for (int[] dir : dirs) {
                int nr = cr + dir[0];
                int nc = cc + dir[1];
                if (nr >= 0 && nr < n && nc >= 0 && nc < n) {
                    if (!visited[nr][nc] && arr[nr][nc] > height) {
                        visited[nr][nc] = true;
                        q.offer(new int[]{nr, nc});
                    }
                }
            }
        }
    }
}
```

|메모리|시간|
|:---:|:---:|
|54712 KB|292 ms|