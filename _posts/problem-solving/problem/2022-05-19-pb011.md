---
layout: post
title: 부분합
# subtitle: 부제목
# description: >
#   설명
date: '2022-05-19 8:30:0'
categories:
  - problem-solving
  - problem
tags: [again,boj,java,two-pointers]
related_posts:
  - 
sitemap: true
published: true
---
# 부분합 🌻🌻

* toc 
{:toc}

- 🌻🌻
- 🔴

## 문제

<https://www.acmicpc.net/problem/1806>{:target="_blank"}

|시간 제한|	메모리 제한|	제출|	정답|	맞힌 사람|	정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|1 초|128 MB|50662|13325|9345|25.246%|

**문제**  
10,000 이하의 자연수로 이루어진 길이 N짜리 수열이 주어진다. 이 수열에서 연속된 수들의 부분합 중에 그 합이 S 이상이 되는 것 중, 가장 짧은 것의 길이를 구하는 프로그램을 작성하시오.

**입력**  
첫째 줄에 N (10 ≤ N < 100,000)과 S (0 < S ≤ 100,000,000)가 주어진다. 둘째 줄에는 수열이 주어진다. 수열의 각 원소는 공백으로 구분되어져 있으며, 10,000이하의 자연수이다.

**출력**  
첫째 줄에 구하고자 하는 최소의 길이를 출력한다. 만일 그러한 합을 만드는 것이 불가능하다면 0을 출력하면 된다.

**예제 입력**  
```
10 15
5 1 3 5 10 7 4 9 2 8
```
**예제 출력**  
```
2
```
## 나의 풀이
두 개의 포인터를 시작 위치에 놓고 sum이 s보다 커질 때까지 p2 늘리고,  
sum이 s보다 커지면 작아질 때까지 p1 늘리면서 sum>=s가 되는 순간마다 길이 최솟값 구함  
(sum은 p1과 p2 사이의 값을 더한 것)

### 틀린 코드


```java
public class Solution {
    public int solution(int n, int s, int[] number) {

        int min = n + 1;
        int p1 = 0;
        int p2 = 0;
        int sum = 0;

        while (p2 < n) {
            if (sum >= s && min > p2 - p1) {
                min = p2 - p1;
            }

            if (sum < s) {
                sum += number[p2++];
            } else {
                sum -= number[p1++];
            }
        }

        return min == n + 1 ? 0 : min;
    }
}
```
끝 부분 계산이 제대로 되지 않는다

반례  
```java
Assertions.assertEquals(2, s.solution(5, 15, new int[]{3, 5, 7, 7, 8}));//위의 코드는 3 나옴
```
### 통과
```java
public class Solution {
    public int solution(int n, int s, int[] number) {

        int min = n + 1;
        int p1 = 0;
        int p2 = 0;
        int sum = 0;

        while (true) {
            if (sum >= s) {
                min = Math.min(min, p2 - p1);
                sum -= number[p1++];
            } else if (p2 == n) {
                break;
            } else {
                sum += number[p2++];
            }
        }
        return min == n + 1 ? 0 : min;
    }
}
```

```java
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(2, s.solution(10, 15, new int[]{5, 1, 3, 5, 10, 7, 4, 9, 2, 8}));
        Assertions.assertEquals(2, s.solution(5, 15, new int[]{3, 5, 7, 7, 8}));
    }
}
```
## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.*;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        StringTokenizer st = new StringTokenizer(br.readLine(), " ");
        int n = Integer.parseInt(st.nextToken());
        int s = Integer.parseInt(st.nextToken());

        st = new StringTokenizer(br.readLine(), " ");

        int[] number = new int[n];
        for (int i = 0; i < n; i++) {
            number[i] = Integer.parseInt(st.nextToken());
        }

        int min = n + 1;
        int p1 = 0;
        int p2 = 0;
        int sum = 0;

        while (true) {
            if (sum >= s) {
                min = Math.min(min, p2 - p1);
                sum -= number[p1++];
            } else if (p2 == n) {
                break;
            } else {
                sum += number[p2++];
            }
        }

        if (min == n + 1) {
            System.out.println(0);
        } else {
            System.out.println(min);
        }
    }
}
```

|메모리|시간|
|:---:|:---:|
|23824KB|292ms|