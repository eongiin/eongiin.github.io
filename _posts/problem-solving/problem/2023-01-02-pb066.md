---
layout: post
title: 순위
# subtitle: 부제목
# description: >
#   설명
date: '2023-01-02 10:30:0'
categories:
  - problem-solving
  - problem
tags: [again,java,floyd-warshall,graph,pgms]
related_posts:
  - 
sitemap: true
published: true
---
# [Lv3] 순위 🌻🌻

* toc 
{:toc}

- 🌻🌻
- 🔴

## 문제

<https://school.programmers.co.kr/learn/courses/30/lessons/49191>{:target="_blank"}

**문제 설명**  
n명의 권투선수가 권투 대회에 참여했고 각각 1번부터 n번까지 번호를 받았습니다. 권투 경기는 1대1 방식으로 진행이 되고, 만약 A 선수가 B 선수보다 실력이 좋다면 A 선수는 B 선수를 항상 이깁니다. 심판은 주어진 경기 결과를 가지고 선수들의 순위를 매기려 합니다. 하지만 몇몇 경기 결과를 분실하여 정확하게 순위를 매길 수 없습니다.

선수의 수 n, 경기 결과를 담은 2차원 배열 results가 매개변수로 주어질 때 정확하게 순위를 매길 수 있는 선수의 수를 return 하도록 solution 함수를 작성해주세요.

**제한사항**  
- 선수의 수는 1명 이상 100명 이하입니다.
- 경기 결과는 1개 이상 4,500개 이하입니다.
- results 배열 각 행 [A, B]는 A 선수가 B 선수를 이겼다는 의미입니다.
- 모든 경기 결과에는 모순이 없습니다.

## 테스트 코드

```java
public class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(2, s.solution(5, new int[][]{
                {4, 3},
                {4, 2},
                {3, 2},
                {1, 2},
                {2, 5}
        }));
    }
}
```

## 나의 풀이

- 이기는 경우 1로 체크
    - ex) 4가 3을 이겼을 때 4행 3열을 1로 체크
- 직접 이기지 않았더라도 4가 3을 이기고 3이 2를 이겼으면 4가 2를 이기므로 이런 경우를 체크해 줌
    - 플로이드 워셜 활용
    - 첫 번째 노드부터 마지막 노드까지 하나씩 확인하면서 이 노드를 통해 결과를 알 수 있는 경우 체크
        - `graph[i][k] == 1 && graph[k][j] == 1` 인 경우 `graph[i][j] = 1`
        - i가 k를 이기고 k가 j를 이기면 i는 j를 이김
- 이기는 경우는 행을 살펴보면 되고 지는 경우는 열을 살펴보면 되므로 합쳐서 개수를 센 다음(`graph[i][j] == 1 || graph[j][i] == 1`)  그 개수가 n-1 이면 모든 결과를 아는 경우에 해당

```java
public class Solution {

    public int solution(int n, int[][] results) {

        int[][] graph = new int[n][n];
        for (int[] result : results) {
            graph[result[0] - 1][result[1] - 1] = 1;
        }

        for (int k = 0; k < n; k++) {
            for (int i = 0; i < n; i++) {
                for (int j = 0; j < n; j++) {
                    if (graph[i][k] == 1 && graph[k][j] == 1) {
                        graph[i][j] = 1;
                    }
                }
            }
        }

        int answer = 0;
        for (int i = 0; i < n; i++) {
            int cnt = 0;
            for (int j = 0; j < n; j++) {
                if (graph[i][j] == 1 || graph[j][i] == 1) {
                    cnt++;
                }
            }
            if (cnt == n - 1) {
                answer++;
            }
        }

        return answer;
    }
}
```

## 결과

### 정확성

```
테스트 1 〉	통과 (0.03ms, 78.8MB)
테스트 2 〉	통과 (0.04ms, 72.9MB)
테스트 3 〉	통과 (0.05ms, 67.4MB)
테스트 4 〉	통과 (0.18ms, 76.3MB)
테스트 5 〉	통과 (0.68ms, 76.8MB)
테스트 6 〉	통과 (1.53ms, 66.5MB)
테스트 7 〉	통과 (7.08ms, 76.4MB)
테스트 8 〉	통과 (11.08ms, 86.8MB)
테스트 9 〉	통과 (10.30ms, 96.9MB)
테스트 10 〉	통과 (9.19ms, 74MB)
```
