---
layout: post
title: 제목
# subtitle: 부제목
# description: >
#   설명
date: '2023-01-10 15:0:0'
categories:
  - problem-solving
  - problem
tags: [boj,java,hash,stack]
related_posts:
  - _posts/problem-solving/problem/2023-01-08-pb072/
sitemap: true
published: true
---
# [Gold3] 오등큰수 🌻🌻

* toc 
{:toc}

- 🌻🌻
- 🟢

## 문제

<https://www.acmicpc.net/problem/17299>{:target="_blank"}

|시간 제한|    메모리 제한|    제출|    정답|    맞힌 사람|    정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|1 초|512  MB|10466|4679|3599|44.345 %|

**문제**  
크기가 N인 수열 A = A1, A2, ..., AN이 있다. 수열의 각 원소 Ai에 대해서 오등큰수 NGF(i)를 구하려고 한다.

Ai가 수열 A에서 등장한 횟수를 F(Ai)라고 했을 때, Ai의 오등큰수는 오른쪽에 있으면서 수열 A에서 등장한 횟수가 F(Ai)보다 큰 수 중에서 가장 왼쪽에 있는 수를 의미한다. 그러한 수가 없는 경우에 오등큰수는 -1이다.

예를 들어, A = [1, 1, 2, 3, 4, 2, 1]인 경우 F(1) = 3, F(2) = 2, F(3) = 1, F(4) = 1이다. A1의 오른쪽에 있으면서 등장한 횟수가 3보다 큰 수는 없기 때문에, NGF(1) = -1이다. A3의 경우에는 A7이 오른쪽에 있으면서 F(A3=2) < F(A7=1) 이기 때문에, NGF(3) = 1이다. NGF(4) = 2, NGF(5) = 2, NGF(6) = 1 이다.

**입력**  
첫째 줄에 수열 A의 크기 N (1 ≤ N ≤ 1,000,000)이 주어진다. 둘째에 수열 A의 원소 A1, A2, ..., AN (1 ≤ Ai ≤ 1,000,000)이 주어진다.

**출력**  
총 N개의 수 NGF(1), NGF(2), ..., NGF(N)을 공백으로 구분해 출력한다.

**예제 입력**

```
7
1 1 2 3 4 2 1
```

**예제 출력**

```
-1 -1 1 2 2 1 -1
```

## 테스트 코드

```java
class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertArrayEquals(new int[]{-1, -1, 1, 2, 2, 1, -1}, s.solution(7, new int[]{1, 1, 2, 3, 4, 2, 1}));
        Assertions.assertArrayEquals(new int[]{-1}, s.solution(1, new int[]{1}));
    }
}
```

## 나의 풀이

- [오큰수](http://127.0.0.1:4000/problem-solving/problem/2023-01-08-pb072/){:target="_blank"} 문제와 거의 똑같은데 빈도수를 체크하는 부분만 추가됨
- 빈도수 체크는 HashMap을 이용하거나 배열을 만들어서 계산할 수 있음
    - 배열로 만드는 게 더 빠름
- 그 뒤는 오큰수와 똑같이 스택을 이용해 값을 비교해 주면 됨

### HashMap

```java
import java.util.HashMap;
import java.util.Map;

public class Solution {
    public int[] solution(int n, int[] arr) {
        Map<Integer, Integer> map = new HashMap<>();

        for (int a : arr) {
            map.merge(a, 1, Integer::sum);
        }

        int[] stack = new int[n];
        int top = -1;
        for (int idx = 1; idx < n; idx++) {
            int prev = map.get(arr[idx - 1]);
            int cur = map.get(arr[idx]);
            if (prev < cur) {
                arr[idx - 1] = arr[idx];
                while (top != -1 && map.get(arr[stack[top]]) < cur) {
                    arr[stack[top--]] = arr[idx];
                }
            } else {
                stack[++top] = idx - 1;
            }
        }

        while (top != -1) {
            arr[stack[top--]] = -1;
        }
        arr[n - 1] = -1;
        return arr;
    }
}
```
### 배열

```java
public class Solution {
    public int[] solution(int n, int[] arr) {
        int[] cnt = new int[1_000_001];

        for (int a : arr) {
            cnt[a]++;
        }

        int[] stack = new int[n];
        int top = -1;
        for (int idx = 1; idx < n; idx++) {
            int prev = cnt[arr[idx - 1]];
            int cur = cnt[arr[idx]];
            if (prev < cur) {
                arr[idx - 1] = arr[idx];
                while (top != -1 && cnt[arr[stack[top]]] < cur) {
                    arr[stack[top--]] = arr[idx];
                }
            } else {
                stack[++top] = idx - 1;
            }
        }

        while (top != -1) {
            arr[stack[top--]] = -1;
        }
        arr[n - 1] = -1;
        return arr;
    }
}
```

## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int[] arr = new int[n];

        int[] cnt = new int[1_000_001];
        StringTokenizer st = new StringTokenizer(br.readLine());
        for (int i = 0; i < n; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
            cnt[arr[i]]++;
        }

        int[] stack = new int[n];
        int top = -1;
        for (int idx = 1; idx < n; idx++) {
            int prev = cnt[arr[idx - 1]];
            int cur = cnt[arr[idx]];
            if (prev < cur) {
                arr[idx - 1] = arr[idx];
                while (top != -1 && cnt[arr[stack[top]]] < cur) {
                    arr[stack[top--]] = arr[idx];
                }
            } else {
                stack[++top] = idx - 1;
            }
        }

        while (top != -1) {
            arr[stack[top--]] = -1;
        }
        arr[n - 1] = -1;

        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < n; i++) {
            sb.append(arr[i]).append(" ");
        }
        System.out.println(sb);
    }
}
```

|메모리|시간|
|:---:|:---:|
|125784 KB|848 ms|