---
layout: post
title: 주차 요금 계산
# subtitle: 부제목
# description: >
#   설명
date: '2022-11-28 14:40:0'
categories:
  - problem-solving
  - problem
tags: [java,data-structure,hash,pgms]
related_posts:
  - 
sitemap: true
published: true
---
# [Lv2] 주차 요금 계산 🌻

* toc 
{:toc}

- 🌻
- 🟢

## 문제

<https://school.programmers.co.kr/learn/courses/30/lessons/92341>{:target="_blank"}

**문제 설명**  
주차장의 요금표와 차량이 들어오고(입차) 나간(출차) 기록이 주어졌을 때, 차량별로 주차 요금을 계산하려고 합니다. 아래는 하나의 예시를 나타냅니다.

- 요금표

|기본 시간(분)|	기본 요금(원)|	단위 시간(분)	|단위 요금(원)|
|180|	5000|	10	|600|
 

- 입/출차 기록

|시각(시:분)|차량 번호|	내역|
|05:34	|5961	|입차|
|06:00	|0000	|입차|
|06:34	|0000	|출차|
|07:59	|5961	|출차|
|07:59	|0148	|입차|
|18:59	|0000	|입차|
|19:09	|0148	|출차|
|22:59	|5961	|입차|
|23:00	|5961	|출차|
 

- 자동차별 주차 요금

|차량 번호|	누적 주차 시간(분)	|주차 요금(원)|
|0000	|34 + 300 = 334	|5000 + ⌈(334 - 180) / 10⌉ x 600 = 14600|
|0148	|670|	5000 +⌈(670 - 180) / 10⌉x 600 = 34400|
|5961	|145 + 1 = 146|	5000|

- 어떤 차량이 입차된 후에 출차된 내역이 없다면, 23:59에 출차된 것으로 간주합니다.
- 0000번 차량은 18:59에 입차된 이후, 출차된 내역이 없습니다. 따라서, 23:59에 출차된 것으로 간주합니다.
- 00:00부터 23:59까지의 입/출차 내역을 바탕으로 차량별 누적 주차 시간을 계산하여 요금을 일괄로 정산합니다.
- 누적 주차 시간이 기본 시간이하라면, 기본 요금을 청구합니다.
- 누적 주차 시간이 기본 시간을 초과하면, 기본 요금에 더해서, 초과한 시간에 대해서 단위 시간 마다 단위 요금을 청구합니다.
  - 초과한 시간이 단위 시간으로 나누어 떨어지지 않으면, 올림합니다.
  - ⌈a⌉ : a보다 작지 않은 최소의 정수를 의미합니다. 즉, 올림을 의미합니다.

주차 요금을 나타내는 정수 배열 fees, 자동차의 입/출차 내역을 나타내는 문자열 배열 records가 매개변수로 주어집니다. 차량 번호가 작은 자동차부터 청구할 주차 요금을 차례대로 정수 배열에 담아서 return 하도록 solution 함수를 완성해주세요.

**제한사항**  
- fees의 길이 = 4
  - fees[0] = 기본 시간(분)
  - 1 ≤ fees[0] ≤ 1,439
  - fees[1] = 기본 요금(원)
  - 0 ≤ fees[1] ≤ 100,000
  - fees[2] = 단위 시간(분)
  - 1 ≤ fees[2] ≤ 1,439
  - fees[3] = 단위 요금(원)
  - 1 ≤ fees[3] ≤ 10,000
- 1 ≤ records의 길이 ≤ 1,000
  - records의 각 원소는 "시각 차량번호 내역" 형식의 문자열입니다.
  - 시각, 차량번호, 내역은 하나의 공백으로 구분되어 있습니다.
  - 시각은 차량이 입차되거나 출차된 시각을 나타내며, HH:MM 형식의 길이 5인 문자열입니다.
    - HH:MM은 00:00부터 23:59까지 주어집니다.
    - 잘못된 시각("25:22", "09:65" 등)은 입력으로 주어지지 않습니다.
  - 차량번호는 자동차를 구분하기 위한, `0'~'9'로 구성된 길이 4인 문자열입니다.
  - l내역은 길이 2 또는 3인 문자열로, IN 또는 OUT입니다. IN은 입차를, OUT은 출차를 의미합니다.
  - records의 원소들은 시각을 기준으로 오름차순으로 정렬되어 주어집니다.
  - records는 하루 동안의 입/출차된 기록만 담고 있으며, 입차된 차량이 다음날 출차되는 경우는 입력으로 주어지지 않습니다.
  - 같은 시각에, 같은 차량번호의 내역이 2번 이상 나타내지 않습니다.
  - 마지막 시각(23:59)에 입차되는 경우는 입력으로 주어지지 않습니다.
  - 아래의 예를 포함하여, 잘못된 입력은 주어지지 않습니다.
    - 주차장에 없는 차량이 출차되는 경우
    - 주차장에 이미 있는 차량(차량번호가 같은 차량)이 다시 입차되는 경우

**입출력 예**  

```java
public class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertArrayEquals(new int[]{14600, 34400, 5000},
                s.solution(
                        new int[]{180, 5000, 10, 600},
                        new String[]{
                                "05:34 5961 IN",
                                "06:00 0000 IN",
                                "06:34 0000 OUT",
                                "07:59 5961 OUT",
                                "07:59 0148 IN",
                                "18:59 0000 IN",
                                "19:09 0148 OUT",
                                "22:59 5961 IN",
                                "23:00 5961 OUT"
                        }
                ));
        Assertions.assertArrayEquals(new int[]{0, 591},
                s.solution(
                        new int[]{120, 0, 60, 591},
                        new String[]{
                                "16:00 3961 IN",
                                "16:00 0202 IN",
                                "18:00 3961 OUT",
                                "18:00 0202 OUT",
                                "23:58 3961 IN"
                        }
                ));
        Assertions.assertArrayEquals(new int[]{14841},
                s.solution(
                        new int[]{1, 461, 1, 10},
                        new String[]{
                                "00:00 1234 IN"
                        }
                ));
    }
}
```

## 나의 풀이

- Parking 클래스를 만들어 가장 최근에 들어간 시각과, 총 주차시간 저장
- HashMap을 이용하여 key는 자동차 번호, value는 Parking 클래스 저장
- Set을 이용하여 현재 주차중인 차 관리
- records를 다 돌았는데 set이 비어있지 않으면 나간 시각을 23:59로 계산하여 총 시간 구해서 저장
- map의 keySet을 오름차순 정렬하고 그 키를 이용해 Parking의 총 주차시간을 가져와서 하나씩 계산하여 정답 배열에 순서대로 저장

```java
import java.util.*;

public class Solution {

    static final int MAX = 23 * 60 + 59;
    int basicTime;
    int basicRate;
    int extraTime;
    int extraRate;

    public int[] solution(int[] fees, String[] records) {
        basicTime = fees[0];
        basicRate = fees[1];
        extraTime = fees[2];
        extraRate = fees[3];

        StringTokenizer st;
        Map<Integer, Parking> map = new HashMap<>();
        Set<Integer> ing = new HashSet<>();
        for (String record : records) {
            st = new StringTokenizer(record);

            String[] timeString = st.nextToken().split(":");
            int time = Integer.parseInt(timeString[0]) * 60 + Integer.parseInt(timeString[1]);
            int number = Integer.parseInt(st.nextToken());
            boolean isIn = st.nextToken().equals("IN");

            if (isIn) {
                Parking parking = map.getOrDefault(number, new Parking(time));
                parking.start = time;
                map.put(number, parking);
                ing.add(number);
            } else {
                Parking parking = map.get(number);
                parking.plusTime(time - parking.start);
                ing.remove(number);
            }
        }

        for (int num : ing) {
            Parking parking = map.get(num);
            parking.plusTime(MAX - parking.start);
        }

        List<Integer> carNumbers = new ArrayList<>(map.keySet());
        Collections.sort(carNumbers);
        int[] answer = new int[carNumbers.size()];
        for (int i = 0; i < answer.length; i++) {
            answer[i] = calcFee(map.get(carNumbers.get(i)).totalTime);
        }
        return answer;
    }

    private int calcFee(int time) {
        if (time <= basicTime) {
            return basicRate;
        }
        return ((time - basicTime + extraTime - 1) / extraTime) * extraRate + basicRate;
    }

    private static class Parking {
        private int start;
        private int totalTime;

        public Parking(int start) {
            this.start = start;
            this.totalTime = 0;
        }

        public void plusTime(int time) {
            totalTime += time;
        }
    }
}
```
### 리팩토링

- 나의 풀이가 불필요한 부분이 있는 것 같아(Set을 이용한 나간 시각이 없는 차 관리, 시간 계산 등) 다른 사람의 코드와 비교해 봤는데 훨씬 깔끔하게 풀 수 있었다
- 먼저 in을 할 때는  -1 곱해서 저장, out 일 땐 그냥 저장해서 주차시간을 한 번에 구할 수 있다
- 마지막에 나간 시각이 찍히지 않은 차들의 주차 시간을 계산할 때도 set으로 굳이 관리해 주지 않아도 현재까지 저장한 총 시간이 음수면 나간 시각이 입력되지 않은 차여서  23*60+59를 더해준 시간이 총 주차 시간이 된다

```java
import java.util.*;

public class Solution {

    static final int MAX = 23 * 60 + 59;
    int basicTime;
    int basicRate;
    int extraTime;
    int extraRate;

    public int[] solution(int[] fees, String[] records) {
        basicTime = fees[0];
        basicRate = fees[1];
        extraTime = fees[2];
        extraRate = fees[3];

        StringTokenizer st;
        Map<Integer, Integer> map = new HashMap<>();
        for (String record : records) {
            st = new StringTokenizer(record);
            int time = toMinute(st.nextToken());
            int number = Integer.parseInt(st.nextToken());
            if (st.nextToken().equals("IN")) {
                time *= -1;
            }
            map.put(number, map.getOrDefault(number, 0) + time);
        }

        List<Integer> carNumbers = new ArrayList<>(map.keySet());
        Collections.sort(carNumbers);
        int[] answer = new int[carNumbers.size()];
        for (int i = 0; i < answer.length; i++) {
            int totalTime = map.get(carNumbers.get(i));
            if (totalTime <= 0) {
                totalTime += MAX;
            }
            answer[i] = calcFee(totalTime);
        }
        return answer;
    }

    private int toMinute(String time) {
        String[] split = time.split(":");
        return Integer.parseInt(split[0]) * 60 + Integer.parseInt(split[1]);
    }

    private int calcFee(int time) {
        if (time <= basicTime) {
            return basicRate;
        }
        return ((time - basicTime + extraTime - 1) / extraTime) * extraRate + basicRate;
    }
}
```

TreeMap을 이용해서 풀면 자동으로 정렬이 되기 때문에 나중에 추가 정렬할 필요없이 바로 value값들을 가져와서 사용할 수 있다

```java
for (int totalTime : map.values()) {
    if (totalTime <= 0) {
        totalTime += MAX;
    }
    answer[idx++] = calcFee(totalTime);
}
```
## 결과

### 정확성

```
테스트 1 〉	통과 (0.54ms, 75MB)
테스트 2 〉	통과 (0.54ms, 71.3MB)
테스트 3 〉	통과 (0.70ms, 80.2MB)
테스트 4 〉	통과 (1.02ms, 68.4MB)
테스트 5 〉	통과 (2.62ms, 72.2MB)
테스트 6 〉	통과 (2.24ms, 75.1MB)
테스트 7 〉	통과 (7.42ms, 87.7MB)
테스트 8 〉	통과 (7.34ms, 84.5MB)
테스트 9 〉	통과 (3.47ms, 80.7MB)
테스트 10 〉	통과 (6.53ms, 79.2MB)
테스트 11 〉	통과 (8.06ms, 75.1MB)
테스트 12 〉	통과 (9.86ms, 81.8MB)
테스트 13 〉	통과 (0.46ms, 76.6MB)
테스트 14 〉	통과 (0.47ms, 72.8MB)
테스트 15 〉	통과 (0.33ms, 73.3MB)
테스트 16 〉	통과 (0.31ms, 69MB)
```