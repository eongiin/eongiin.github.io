---
layout: post
title: LCS
# subtitle: 부제목
# description: >
#   설명
date: '2022-06-02 23:0:0'
categories:
  - problem-solving
  - problem
tags: [again,boj,dp,essential,java,string]
related_posts:
  - 
sitemap: true
published: true
---
# LCS 🌻🌼

* toc 
{:toc}

- 🌻🌼
- 🔴

## 문제

<https://www.acmicpc.net/problem/9251>{:target="_blank"}

|시간 제한|	메모리 제한|	제출|	정답|	맞힌 사람|	정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|0.4 초|256 MB|51683|20837|15312|40.335%|

**문제**  
LCS(Longest Common Subsequence, 최장 공통 부분 수열)문제는 두 수열이 주어졌을 때, 모두의 부분 수열이 되는 수열 중 가장 긴 것을 찾는 문제이다.

예를 들어, ACAYKP와 CAPCAK의 LCS는 ACAK가 된다.

**입력**  
첫째 줄과 둘째 줄에 두 문자열이 주어진다. 문자열은 알파벳 대문자로만 이루어져 있으며, 최대 1000글자로 이루어져 있다.

**출력**  
첫째 줄에 입력으로 주어진 두 문자열의 LCS의 길이를 출력한다.

**예제 입력**

```
ACAYKP
CAPCAK
```

**예제 출력**

```
4
```

## 나의 풀이

- 비교하고 있는 문자열이 같다면 `dp[i][j] = dp[i-1][j-1] + 1`
- 비교하고 있는 문자열이 다르다면 `dp[i][j] = Math.max(dp[i-1][j-1], dp[i][j-1])`
- 문자열은 0부터 계산하므로 `i-1`, `j-1`로 비교

```java
public class Solution {
    public int solution(String s1, String s2) {
        int n = s1.length();
        int m = s2.length();
        int[][] dp = new int[n + 1][m + 1];

        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                if (s1.charAt(i - 1) == s2.charAt(j - 1)) {
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                } else {
                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
                }
            }
        }
        return dp[n][m];
    }
}
```

```java
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(4, s.solution("ACAYKP", "CAPCAK"));
    }
}
```

## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {

    public static void main(String[] args) throws IOException {

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        String s1 = br.readLine();
        String s2 = br.readLine();

        int n = s1.length();
        int m = s2.length();
        int[][] dp = new int[n + 1][m + 1];

        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                if (s1.charAt(i - 1) == s2.charAt(j - 1)) {
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                } else {
                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
                }
            }
        }
        System.out.println(dp[n][m]);
    }
}
```

|메모리|시간|
|:---:|:---:|
|18496KB|156ms|