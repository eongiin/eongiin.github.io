---
layout: post
title: 01타일
# subtitle: 부제목
# description: >
#   설명
date: '2022-10-20 0:15:0'
categories:
  - problem-solving
  - problem
tags: [boj,dp,java]
related_posts:
  -
sitemap: true
published: true
---
# 01타일 🌻

* toc
{:toc}
- 🌻
- 🟢

## 문제

<https://www.acmicpc.net/problem/1904>{:target="_blank"}

|시간 제한|    메모리 제한|    제출|    정답|    맞힌 사람|    정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|0.75 초|256 MB|72984|23769|18872|31.920%|

**문제**  
지원이에게 2진 수열을 가르쳐 주기 위해, 지원이 아버지는 그에게 타일들을 선물해주셨다. 그리고 이 각각의 타일들은 0 또는 1이 쓰여 있는 낱장의 타일들이다.

어느 날 짓궂은 동주가 지원이의 공부를 방해하기 위해 0이 쓰여진 낱장의 타일들을 붙여서 한 쌍으로 이루어진 00 타일들을 만들었다. 결국 현재 1 하나만으로 이루어진 타일 또는 0타일을 두 개 붙인 한 쌍의 00타일들만이 남게 되었다.

그러므로 지원이는 타일로 더 이상 크기가 N인 모든 2진 수열을 만들 수 없게 되었다. 예를 들어, N=1일 때 1만 만들 수 있고, N=2일 때는 00, 11을 만들 수 있다. (01, 10은 만들 수 없게 되었다.) 또한 N=4일 때는 0011, 0000, 1001, 1100, 1111 등 총 5개의 2진 수열을 만들 수 있다.

우리의 목표는 N이 주어졌을 때 지원이가 만들 수 있는 모든 가짓수를 세는 것이다. 단 타일들은 무한히 많은 것으로 가정하자.

**입력**  
첫 번째 줄에 자연수 N이 주어진다. (1 ≤ N ≤ 1,000,000)

**출력**  
첫 번째 줄에 지원이가 만들 수 있는 길이가 N인 모든 2진 수열의 개수를 15746으로 나눈 나머지를 출력한다.

**예제 입력**  
```
4
```
**예제 출력**  
```
5
```
## 나의 풀이

- `dp[i] = dp[i-2] + dp[i-1]`
- 피보나치 수
- n=1인 경우 바로 1 반환
  - 따로 처리하지 않으면 `dp[2]=2`에서 `ArrayIndexOutOfBounds` 에러 발생

```java
public class Solution {
    final static int mod = 15746;

    public long solution(int n) {

        if (n == 1) {
            return 1;
        }

        long[] dp = new long[n + 1];
        dp[1] = 1;
        dp[2] = 2;

        for (int i = 3; i < n + 1; i++) {
            dp[i] = (dp[i - 1] + dp[i - 2]) % mod;
        }
        return dp[n];
    }
}
```

```java
class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(5, s.solution(4));
    }
}
```

- 굳이 배열을 만들지 않고 그냥 변수 세 개로 처리할 수 있다 (아래 코드 확인)
  - num1, num2, sum
  - n=2인 경우에도 바로 2 반환하도록 변경
  - 메모리를 훨씬 절약할 수 있다

## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {
    final static int mod = 15746;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        System.out.println(fibo(n));
    }

    private static int fibo(int n) {
        if (n <= 2) {
            return n;
        }

        int num1 = 1;
        int num2 = 2;
        int sum = 0;

        for (int i = 3; i < n + 1; i++) {
            sum = (num1 + num2) % mod;
            num1 = num2;
            num2 = sum;
        }
        return sum;
    }
}
```

|메모리|시간|
|:---:|:---:|
|14280KB|136ms|