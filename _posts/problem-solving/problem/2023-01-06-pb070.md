---
layout: post
title: 제목
# subtitle: 부제목
# description: >
#   설명
date: '2023-01-06 10:0:0'
categories:
  - problem-solving
  - problem
tags: [boj,java,stack,string]
related_posts:
  - 
sitemap: true
published: true
---
# [Gold4] 문자열 폭발 🌻🌼

* toc 
{:toc}

- 🌻🌼
- 🟢

## 문제

<https://www.acmicpc.net/problem/9935>{:target="_blank"}

|시간 제한|    메모리 제한|    제출|    정답|    맞힌 사람|    정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|2 초|128  MB|51498|12669|8878|24.622%|

**문제**  
상근이는 문자열에 폭발 문자열을 심어 놓았다. 폭발 문자열이 폭발하면 그 문자는 문자열에서 사라지며, 남은 문자열은 합쳐지게 된다.

폭발은 다음과 같은 과정으로 진행된다.

- 문자열이 폭발 문자열을 포함하고 있는 경우에, 모든 폭발 문자열이 폭발하게 된다. 남은 문자열을 순서대로 이어 붙여 새로운 문자열을 만든다.
- 새로 생긴 문자열에 폭발 문자열이 포함되어 있을 수도 있다.
- 폭발은 폭발 문자열이 문자열에 없을 때까지 계속된다.

상근이는 모든 폭발이 끝난 후에 어떤 문자열이 남는지 구해보려고 한다. 남아있는 문자가 없는 경우가 있다. 이때는 "FRULA"를 출력한다.

폭발 문자열은 같은 문자를 두 개 이상 포함하지 않는다.

**입력**  
첫째 줄에 문자열이 주어진다. 문자열의 길이는 1보다 크거나 같고, 1,000,000보다 작거나 같다.

둘째 줄에 폭발 문자열이 주어진다. 길이는 1보다 크거나 같고, 36보다 작거나 같다.

두 문자열은 모두 알파벳 소문자와 대문자, 숫자 0, 1, ..., 9로만 이루어져 있다.

**출력**  
첫째 줄에 모든 폭발이 끝난 후 남은 문자열을 출력한다.

**예제 입력1**

```
mirkovC4nizCC44
C4
```

**예제 출력1**

```
mirkovniz
```

**예제 입력2**

```
12ab112ab2ab
12ab
```

**예제 출력2**

```
FRULA
```

## 테스트 코드

```java
class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals("mirkovniz", s.solution("mirkovC4nizCC44", "C4"));
        Assertions.assertEquals("miCrkovniz", s.solution("miCrkovC4nizCC44", "C4"));
        Assertions.assertEquals("FRULA", s.solution("12ab112ab2ab", "12ab"));
    }
}
```

## 나의 풀이
### StringBuilder

- 일단 sb에 문자 하나씩 추가
- sb 크기가 폭발 문자 크기 이상이 될 때(sb에 포함된 문자열이 폭발 문자일 가능성이 있을 때) 가장 끝부분에서 폭발 문자 크기만큼 검사
    - 폭발 문자와 같다면 삭제

```java
public class Solution {

    public String solution(String s, String bomb) {
        StringBuilder sb = new StringBuilder();
        int bombLen = bomb.length();

        for (int i = 0; i < s.length(); i++) {
            sb.append(s.charAt(i));

            if (sb.length() >= bombLen) {
                boolean isBomb = true;
                for (int j = 0; j < bombLen; j++) {
                    if (sb.charAt(sb.length() - bombLen + j) != bomb.charAt(j)) {
                        isBomb = false;
                        break;
                    }
                }
                if (isBomb) {
                    sb.delete(sb.length() - bombLen, sb.length());
                }
            }
        }
        return sb.length() == 0 ? "FRULA" : sb.toString();
    }
}
```

### 배열에 덮어쓰기
- 방식은 위와 완전히 동일한데 입력할 위치를 top에 저장해 두고 폭발 문자를 만나면 폭발 문자 길이만큼 top 값을 줄여서 자동으로 지워지게 함
- 처음 입력받은 string 배열에 그대로 덮어씀
- [0,top) 만 출력

```java
import java.util.Arrays;

public class Solution {
    public String solution(String s, String b) {

        char[] string = s.toCharArray();
        char[] bomb = b.toCharArray();

        int sLen = string.length;
        int bombLen = bomb.length;

        int idx = 0;
        int top = 0;

        while (idx < sLen) {
            string[top++] = string[idx++];
            if (top >= bombLen) {
                int i = 1;
                for (; i <= bombLen; i++) {
                    if (string[top - i] != bomb[bombLen - i]) {
                        break;
                    }
                }
                if (i == bombLen + 1) {
                    if (i == bombLen + 1) {
                        top -= bombLen;
                    }
                }
            }
        }

        return top == 0 ? "FRULA" : new String(Arrays.copyOfRange(string, 0, top));
    }
}
```
## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Arrays;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        char[] string = br.readLine().toCharArray();
        char[] bomb = br.readLine().toCharArray();


        int sLen = string.length;
        int bombLen = bomb.length;

        int idx = 0;
        int top = 0;

        while (idx < sLen) {
            string[top++] = string[idx++];
            if (top >= bombLen) {
                int i = 1;
                for (; i <= bombLen; i++) {
                    if (string[top - i] != bomb[bombLen - i]) {
                        break;
                    }
                }
                if (i == bombLen + 1) {
                    top -= bombLen;
                }
            }
        }

        System.out.println(top == 0 ? "FRULA" : new String(Arrays.copyOfRange(string, 0, top)));
    }
}
```



|메모리|시간|
|:---:|:---:|
|24992 KB|336 ms|