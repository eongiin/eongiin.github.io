---
layout: post
title: 전력망을 둘로 나누기
# subtitle: 부제목
# description: >
#   설명
date: '2022-11-21 11:0:0'
categories:
  - problem-solving
  - problem
tags: [again,pgms,brute-force,dfs,graph,java]
related_posts:
  - 
sitemap: true
published: true
---
# [Lv2] 전력망을 둘로 나누기 🌻🌻

* toc 
{:toc}

- 🌻🌻
- 🔴

## 문제

<https://school.programmers.co.kr/learn/courses/30/lessons/86971>{:target="_blank"}

**문제 설명**  
n개의 송전탑이 전선을 통해 하나의 트리 형태로 연결되어 있습니다. 당신은 이 전선들 중 하나를 끊어서 현재의 전력망 네트워크를 2개로 분할하려고 합니다. 이때, 두 전력망이 갖게 되는 송전탑의 개수를 최대한 비슷하게 맞추고자 합니다.

송전탑의 개수 n, 그리고 전선 정보 wires가 매개변수로 주어집니다. 전선들 중 하나를 끊어서 송전탑 개수가 가능한 비슷하도록 두 전력망으로 나누었을 때, 두 전력망이 가지고 있는 송전탑 개수의 차이(절대값)를 return 하도록 solution 함수를 완성해주세요.

**제한사항**  
- n은 2 이상 100 이하인 자연수입니다.
- wires는 길이가 n-1인 정수형 2차원 배열입니다.
    - wires의 각 원소는 [v1, v2] 2개의 자연수로 이루어져 있으며, 이는 전력망의 v1번 송전탑과 v2번 송전탑이 전선으로 연결되어 있다는 것을 의미합니다.
    - 1 ≤ v1 < v2 ≤ n 입니다.
    - 전력망 네트워크가 하나의 트리 형태가 아닌 경우는 입력으로 주어지지 않습니다.

**입출력 예**  

```java
class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(3, s.solution(9, new int[][]{
                {1, 3},
                {2, 3},
                {3, 4},
                {4, 5},
                {4, 6},
                {4, 7},
                {7, 8},
                {7, 9}
        }));

        Assertions.assertEquals(0, s.solution(4, new int[][]{
                {1, 2},
                {2, 3},
                {3, 4}
        }));

        Assertions.assertEquals(1, s.solution(7, new int[][]{
                {1, 2},
                {2, 7},
                {3, 7},
                {3, 4},
                {4, 5},
                {6, 7}
        }));
    }
}
```

## 나의 풀이

- 먼저 그래프를 만들어주고, 하나씩 방문하면서 현재 방문한 개수(cnt)와 방문하지 않은 개수(n-cnt)의 차이를 매번 구해서 가장 작은 값이 답이 된다

```java
import java.util.ArrayList;

public class Solution {

    ArrayList<Integer>[] graph;
    boolean[] visited;
    int answer;
    int n;

    public int solution(int n, int[][] wires) {
        this.n = n;
        answer = n;
        graph = new ArrayList[n + 1];
        visited = new boolean[n + 1];

        for (int i = 1; i < n + 1; i++) {
            graph[i] = new ArrayList<>();
        }

        for (int[] wire : wires) {
            graph[wire[0]].add(wire[1]);
            graph[wire[1]].add(wire[0]);
        }

        dfs(1);
        return answer;
    }

    private int dfs(int cur) {
        int cnt = 1;
        visited[cur] = true;
        for (int adj : graph[cur]) {
            if (visited[adj]) {
                continue;
            }
            cnt += dfs(adj);
        }
        answer = Math.min(answer, Math.abs(cnt - (n - cnt)));
        return cnt;
    }
}
```

## 결과

### 정확성

```
테스트 1 〉	통과 (0.36ms, 85.8MB)
테스트 2 〉	통과 (0.34ms, 70.3MB)
테스트 3 〉	통과 (0.37ms, 80.1MB)
테스트 4 〉	통과 (0.83ms, 93.2MB)
테스트 5 〉	통과 (0.73ms, 74.3MB)
테스트 6 〉	통과 (0.07ms, 67.3MB)
테스트 7 〉	통과 (0.10ms, 76.6MB)
테스트 8 〉	통과 (0.12ms, 72.5MB)
테스트 9 〉	통과 (0.19ms, 69.4MB)
테스트 10 〉	통과 (0.25ms, 79.4MB)
테스트 11 〉	통과 (0.38ms, 67.3MB)
테스트 12 〉	통과 (0.39ms, 89.5MB)
테스트 13 〉	통과 (0.34ms, 67.7MB)
```