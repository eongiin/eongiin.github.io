---
layout: post
title: 이친수
# subtitle: 부제목
# description: >
#   설명
date: '2022-06-01 21:40:0'
categories:
  - problem-solving
  - problem
tags: [boj,dp,essential,java]
related_posts:
  - 
sitemap: true
published: true
---
# 이친수 🌻

* toc 
{:toc}

- 🌻
- 🟢

## 문제

<https://www.acmicpc.net/problem/2193>{:target="_blank"}

|시간 제한|	메모리 제한|	제출|	정답|	맞힌 사람|	정답 비율|
|:---:|:---:|:---:|:---:|:---:|:---:|
|2 초|128 MB|72314|29947|22409|39.677%|

**문제**  
0과 1로만 이루어진 수를 이진수라 한다. 이러한 이진수 중 특별한 성질을 갖는 것들이 있는데, 이들을 이친수(pinary number)라 한다. 이친수는 다음의 성질을 만족한다.

1. 이친수는 0으로 시작하지 않는다.
2. 이친수에서는 1이 두 번 연속으로 나타나지 않는다. 즉, 11을 부분 문자열로 갖지 않는다.

예를 들면 1, 10, 100, 101, 1000, 1001 등이 이친수가 된다. 하지만 0010101이나 101101은 각각 1, 2번 규칙에 위배되므로 이친수가 아니다.

N(1 ≤ N ≤ 90)이 주어졌을 때, N자리 이친수의 개수를 구하는 프로그램을 작성하시오.

**입력**  
첫째 줄에 N이 주어진다.

**출력**  
첫째 줄에 N자리 이친수의 개수를 출력한다.


**예제 입력**

```
3
```

**예제 출력**

```
2
```

## 나의 풀이

- `dp[i] = dp[i - 2] + dp[i - 1]`
- `int` 형으로 dp 배열을 선언하면 범위를 벗어나 오답이 나온다 → `long`으로 선언

```java
public class Solution {
    public long solution(int n) {
        long[] dp = new long[n + 1];

        dp[0] = 0;
        dp[1] = 1;

        for (int i = 2; i <= n; i++) {
            dp[i] = dp[i - 2] + dp[i - 1];
        }
        return dp[n];
    }
}
```

```java
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class SolutionTest {
    @Test
    void test() {
        Solution s = new Solution();
        Assertions.assertEquals(2, s.solution(3));
    }
}
```

## 최종 코드

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {

    public static void main(String[] args) throws IOException {

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int n = Integer.parseInt(br.readLine());
        long[] dp = new long[n + 1];

        dp[0] = 0;
        dp[1] = 1;

        for (int i = 2; i <= n; i++) {
            dp[i] = dp[i - 2] + dp[i - 1];
        }

        System.out.println(dp[n]);
    }
}
```

|메모리|시간|
|:---:|:---:|
|14252KB|128ms|