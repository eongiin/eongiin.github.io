---
layout: post
title: July 2nd 2022
subtitle: 2022.07.11~07.17
# description: >
#   2022.07. 둘째 주 알게 된 내용
date: '2022-07-11 0:0:0'
categories:
  - til
tags: [java,spring,pj03]
related_posts:
  - 
sitemap: false
published: true
---
# [ 22.07.2nd ] : Spring, Project

* toc 
{:toc}

# 11 Mon
## 🚀 LMS Project

### 공통 화면 (thymeleaf)
- `templates` - `fragments` 생성 - `layout.html` 생성

```html
<div th:fragment="fragment-body-menu">
    <div>
        <a href="/member/register">회원 가입</a>
        |
        <a href="/member/info">회원 정보</a>
        |
        <a href="/member/login">로그인</a>
        |
        <a href="/member/logout">로그아웃</a>
    </div>
    <hr/>
</div>
```
- 다른 곳에서 쓸 때는 아래와 같이 쓰면 됨

```html
<div th:replace="/fragments/layout.html :: fragment-body-menu"></div>
```

### 비밀번호 찾기(초기화)
1. 아이디(메일)과 이름을 입력하고 비밀번호 초기화를 누르면 해당 정보가 있는지 확인하고, 있으면 그 메일로 초기화할 수 있는 페이지 링크 주소가 담긴 메일을 보낸다
2. 메일을 보낼 때, UUID를 생성해서 `ResetPasswordKey`로 설정하고, 현재 날짜에서 +1한 날짜를 `ResetPasswordLimitDt`로 설정해서 저장한다
3. 메일로 받은 링크로 접속했을 때 유효한 UUID 값인지 확인하고(`checkResetPassword`) 맞으면 초기화할 수 있는 폼을 보여주고 아니면 메인으로 돌아갈 수 있는 링크를 보여준다
4. 초기화 폼이 성공적으로 나오면 비밀번호를 입력한 후, 비밀번호를 맞게 입력했는지 확인 후 지정한 값으로 비밀번호를 재설정한다 (DB에 저장되어 있는 UUID, 초기화-날짜 초기화 )


- 비밀번호 초기화 버튼 눌렀을 때 초기화할 수 있는 페이지 링크를 메일로 보내는 부분  
`MemberController.java`  
    
```java
@PostMapping("/member/find/password")
public String findPasswordSubmit(
        Model model,
        ResetPasswordInput parameter
) {
    boolean result = false;
    try {
        result = memberService.sendResetPassword(parameter);
    } catch (Exception e) {
    }
    model.addAttribute("result", result);
    return "member/find_password_result";
}
```

`MemberServiceImpl.java`

```java
@Override
public boolean sendResetPassword(ResetPasswordInput parameter) {
    Optional<Member> optionalMember =
            memberRepository.findByUserIdAndUserName(
                    parameter.getUserId(),
                    parameter.getUserName()
            );

    if (optionalMember.isEmpty()) {
        throw new UsernameNotFoundException("회원 정보가 존재하지 않습니다.");
    }

    Member member = optionalMember.get();
    String uuid = UUID.randomUUID().toString();

    member.setResetPasswordKey(uuid);
    member.setResetPasswordLimitDt(LocalDateTime.now().plusDays(1));
    memberRepository.save(member);

    return mailComponents.sendMail(
            parameter.getUserId(),
            "[fastlms] 비밀번호 초기화 메일 입니다",
            "<p>fastlms 비밀번호 초기화 메일 입니다.</p>"
                    + "<p>아래 링크를 클릭하셔서 비밀번호를 초기화 해주세요.</p>"
                    + "<div><a target='_blank' href='http://localhost:8080/member/reset/password?id="
                    + uuid
                    + "'>비밀번호 초기화 링크</a></div>"
    );
}
```

- 사용자가 설정한 비밀번호로 설정  
`MemberController.java`

```java
@PostMapping("/member/reset/password")
public String resetPasswordSubmit(Model model, ResetPasswordInput parameter) {

    boolean result = false;
    try {
        result = memberService.resetPassword(parameter.getId(), parameter.getPassword());
    } catch (Exception e) {
    }

    model.addAttribute("result", result);
    return "member/reset_password_result";
}
```

`MemberServiceImpl`

```java
@Override
public boolean resetPassword(String uuid, String password) {

    Optional<Member> optionalMember = memberRepository.findByResetPasswordKey(uuid);
    if (optionalMember.isEmpty()) {
        throw new UsernameNotFoundException("회원 정보가 존재하지 않습니다");
    }

    Member member = optionalMember.get();

    if (member.getResetPasswordLimitDt() == null) {
        throw new RuntimeException("유효한 날짜가 아닙니다.");//나중에 수정
    }

    if (member.getResetPasswordLimitDt().isBefore(LocalDateTime.now())) {
        throw new RuntimeException("유효한 날짜가 아닙니다.");//나중에 수정
    }

    String encPassword = BCrypt.hashpw(password, BCrypt.gensalt());
    member.setPassword(encPassword);
    member.setResetPasswordKey("");
    member.setResetPasswordLimitDt(null);
    memberRepository.save(member);

    return true;
}
```


### 비밀번호 확인

- jQuery를 이용해서 간단하게 해 볼 예정
    - [jQuery cdn](https://releases.jquery.com/){:target="_blank"}
    - `minifed` - copy

```html
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"
            integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
            crossorigin="anonymous">
    </script>
    <script>
        $(document).ready(function () {
            $('form').on('submit', function () {
                var password = $(this).find('input[name=password]').val();
                var rePassword = $(this).find('input[name=rePassword]').val();
                if (password != rePassword) {
                    alert('비밀번호값이 일치하지 않습니다. ');
                    return false;
                }
            })
        });
    </script>
```

### 비밀번호 초기화 코드
- [전체 코드](https://github.com/eongiin/fastlms/commit/e0f69162f86b6050b21817be18fd4a723df04866){:target="_blank"}

## 🔔 Tomorrow
- LMS Project

# 12 Tue
## 🚀 LMS Project
### 관리자 로그인
1. Member Entity에 관리자를 구분할 수 있도록 `adminYn` 칼럼 추가
2. 로그인할 때 member의 adminYn을 확인해서 true면 관리자 롤 부여
3. 관리자만 admin 페이지 접근 가능하도록 설정

<br>

- 로그인할 때 member의 adminYn을 확인해서 true면 관리자 롤 부여  
`MemberServiceImpl.java` - `loadUserByUsername`

```java
if (member.isAdminYn()) {
    grantedAuthorities.add(new SimpleGrantedAuthority("ROLE_ADMIN"));
}
```

- 관리자만 admin 페이지 접근 가능하도록 설정  
`SecurityConfiguration` - `configure`

```java
http.authorizeRequests()
        .antMatchers("/admin/**")
        .hasAuthority("ROLE_ADMIN");
```

#### 전체 코드
- [코드](https://github.com/eongiin/fastlms/commit/056afb560dd6e825825082366466193f3d82cb7d){:target="_blank"}

### 회원 목록

- 보여줄 데이터 : 번호, 아이디, 연락처, 이메일 인증 여부, 가입일, 관리자여부

```html
<tr th:each="x : ${list}">
    <td>1</td>
    <td th:text="${x.userId}">eongiin@naver.com</td>
    <td th:text="${x.userName}">달미</td>
    <td th:text="${x.phone}">010-1010-1010</td>
    <td>
        <p th:if="${x.emailAuthYn}">Y</p>
        <p th:if="${x.emailAuthYn eq false}">N</p>
    </td>
    <td>
        <p th:text="${x.regDt}">2022.01.01</p>
    </td>
    <td>
        <p th:if="${x.adminYn}">Y</p>
        <p th:if="${x.adminYn eq false}">N</p>
    </td>
</tr>
```
(번호는 나중에 수정 예정)

- JPA로 회원 정보 가져오기
    - `memberRepository.findAll()`

#### mybatis
- 쉽게 페이징 처리를 하기 위해 사용해 볼 예정

##### 설정

- pom.xml - mybatis추가

```xml
<dependency>
    <groupId>org.mybatis.spring.boot</groupId>
    <artifactId>mybatis-spring-boot-starter</artifactId>
    <version>2.2.2</version>
</dependency>
```

- application.yml

```yml
mybatis:
  mapper-locations: mybatis/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
```

##### 회원 목록 가져오기
- `admin`-`mapper`에 `MemberMapper` 인터페이스를 만들고 `resources`-`mybatis`에 `MemberMapper.xml`을 만든다  
- `MemberServiceImpl` 에서 `memberMapper.selectList()`로 가져오면 된다

`MemberMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zerobase.fastlms.admin.mapper.MemberMapper">
    <select id="selectList" resultType="com.zerobase.fastlms.admin.dto.MemberDto">
        select *
        from member;
    </select>
</mapper>
```

```java
@Mapper
public interface MemberMapper {
    List<MemberDto> selectList();
}
```

#### 회원 검색
1. 검색타입(전체, 아이디, 이름, 연락처 ), 검색어  
→ Get-RequestParam으로 넘어온 값 MemberParam 클래스 만들어서(model) 매핑
2. 케이스별 쿼리 작성

- searchFrom

```html
<div class="search-form">
    <form method="get">
        <select name="searchType">
            <option value="all">전체</option>
            <option th:selected="${#strings.equals(param.searchType, 'userId')}"
                    value="userId">아이디
            </option>
            <option th:selected="${#strings.equals(param.searchType, 'userName')}"
                    value="userName">이름
            </option>
            <option th:selected="${#strings.equals(param.searchType, 'phone')}"
                    value="phone">연락처
            </option>
        </select>
        <input th:value="${param.searchValue}" type="search"
                name="searchValue" placeholder="검색어 입력">
        <button type="submit">검색</button>
    </form>
</div>
```

- AdminMemberController

```java
@GetMapping("/admin/member/list.do")
public String list(Model model, MemberParam parameter) {
    List<MemberDto> members = memberService.list(parameter);
    model.addAttribute("list", members);
    return "admin/member/list";
}
```

- MemberMapper.xml

```xml
<select id="selectList"
        parameterType="com.zerobase.fastlms.admin.model.MemberParam"
        resultType="com.zerobase.fastlms.admin.dto.MemberDto">

    select *
    from member
    where 1 = 1
    <if test="searchType != null and searchValue != null">
        <choose>
            <when test="searchType == 'userId'">
                and user_id like concat('%', #{searchValue}, '%')
            </when>
            <when test="searchType == 'userName'">
                and user_name like concat('%', #{searchValue}, '%')
            </when>
            <when test="searchType == 'phone'">
                and phone like concat('%', #{searchValue}, '%')
            </when>
            <otherwise>
                and
                (
                user_id like concat('%', #{searchValue}, '%')
                or
                user_name like concat('%', #{searchValue}, '%')
                or
                phone like concat('%', #{searchValue}, '%')
                )
            </otherwise>
        </choose>
    </if>
</select>
```

#### 전체 코드
- [코드](https://github.com/eongiin/fastlms/commit/69dc237ce65035fbaa1c9ec4dfd78b9ad5ed40ba){:target="_blank"}
- 페이징은 따로 다시 공부하기

### 회원 상세 및 상태 처리

#### 회원 상태
- 상태
    - 가입요청
    - 이용중
    - 정지
    - 탈퇴

- 가입할 때 가입요청 상태로 초기화
    - `MemberServiceImpl` - `register`
        - `.userStatus(Member.MEMBER_STATUS_REQ)` (Member를 builder로 생성해서 save하는 부분)
- 이메일 인증 후 이용중으로 변경
    - `MemberServiceImpl` - `emailAuth`
        - `member.setUserStatus(Member.MEMBER_STATUS_ING);` 

- 관리자는 회원 상세 정보에서 회원의 상태 변경 가능

```html
<tr>
    <th>회원 상태</th>
    <td>
        <p>
            현재상태: <span th:text="${member.userStatus}"></span>
        </p>
        <div>
            <form method="post" action="/admin/member/status.do">
                <input th:value="${member.userId}" type="hidden" name="userId"/>
                <select name="userStatus">
                    <option value="REQ">가입승인중</option>
                    <option value="ING">정상이용중</option>
                    <option value="STOP">정지중</option>
                </select>
                <button type="submit">상태 변경</button>
            </form>
        </div>
    </td>
</tr>
```

```java
    @PostMapping("/admin/member/status.do")
    public String status(Model model, MemberInput parameter) {
        memberService.updateStatus(parameter.getUserId(), parameter.getUserStatus());
        return "redirect:/admin/member/detail.do?userId=" + parameter.getUserId();
    }
```

```java
@Override
public boolean updateStatus(String userId, String userStatus) {

    Optional<Member> optionalMember = memberRepository.findById(userId);
    if (!optionalMember.isPresent()) {
        throw new UsernameNotFoundException("회원 정보가 존재하지 않습니다.");
    }

    Member member = optionalMember.get();
    member.setUserStatus(userStatus);
    memberRepository.save(member);

    return true;
}
```


#### 전체 코드
- [코드](https://github.com/eongiin/fastlms/commit/ea8d5694f557651ecc2e1459641b3587a55c9f74){:target="_blank"}
- 상세 정보 페이지 구현, 패스워드 초기화는 위 코드 참조

## 🔔 Tomorrow
- LMS Project

# 13 Wed
## 🚀 LMS Project
### 카테고리 화면

- [전체 코드](https://github.com/eongiin/fastlms/commit/739d6a6025a4817bf82e18b62d95ef979e5e5487){:target="_blank"}

### 강좌 목록
- 강좌 상세 정보 등록 및 수정
- 강좌 삭제 및 일괄 선택, 삭제
- 스마트에디터
- [전체코드](https://github.com/eongiin/fastlms/commit/77858b0835c62cbeb6faed00558ae875eaa45bdb){:target="_blank"}

## 🔔 Tomorrow

- PS

# 16 Sat

## 🚀 LMS Project

### 강좌 목록 (front)

- 강좌 목록
    - [코드](https://github.com/eongiin/fastlms/commit/2d81eb7fb282fdefa9f2cccbfd84daac8c0a6e1e){:target="_blank"}
- 강좌 상세 정보
    - [코드](https://github.com/eongiin/fastlms/commit/62979d4061b5a8a519d37c2fb6d9ae3479f4e5fd){:target="_blank"}

### 강좌 신청

- (front) 강좌 신청
    - [코드](https://github.com/eongiin/fastlms/commit/db7a92f3e13a3a2f4ccde9966fc3ecf39f54516b){:target="_blank"}
- (back) 강좌 신청 처리
    - [코드](https://github.com/eongiin/fastlms/commit/4318d3511bfb6e87e484d2250b9d0efb9f42e981){:target="_blank"}

### 회원 정보 수정
- 비밀번호 변경
    - [코드](https://github.com/eongiin/fastlms/commit/06046a76b8b865d6f8175ea210259a22aac1adde){:target="_blank"}
- 정보 수정 (전화번호)
    - [코드](https://github.com/eongiin/fastlms/commit/74275c64f433afa55260c2512bff74ca8f7981de){:target="_blank"}
- 다음 우편번호 API를 이용한 주소 정보 수정
    - [코드](https://github.com/eongiin/fastlms/commit/b736518b5c6471731d1a8f22695654430d8bbc2f){:target="_blank"}
    - [다음 API](https://postcode.map.daum.net/guide){:target="_blank"}
- 수강 신청 취소
    - [코드](https://github.com/eongiin/fastlms/commit/58e007788dde58f402abd91b834fa01e25755340){:target="_blank"}
- 회원 탈퇴
    - [코드](https://github.com/eongiin/fastlms/commit/d49b4e2e65cdb78eb8f4e5d21740e098caab2212){:target="_blank"}
    - 정보를 아예 삭제하는 건 아니고 리셋
    - 이메일 남겨놓고 상태를 탈퇴로 바꿈

## 🔔 Tomorrow
- LMS 프로젝트

# 17 Sun

## 🚀 LMS Project

### 강좌 관리

- 강좌별 신청 내역
    - [코드](https://github.com/eongiin/fastlms/commit/363d5774996e654c3168aa46fada051585d44f9d){:target="_blank"}
- 파일 업로드
    - [코드](https://github.com/eongiin/fastlms/commit/47b749af01a4b85c117e8e2f22f35c32e6da827c){:target="_blank"}
        - 강좌 수정 부분 추가 처리 필요(파일 선택)
    - 톰캣 추가
        - ![image](https://user-images.githubusercontent.com/90626691/179381666-b8f9e0fd-0914-49dd-b7cc-4dd867eee6a1.png)
        - ![image](https://user-images.githubusercontent.com/90626691/179381678-6c69e979-de7a-4003-9d2d-4553c6df60f5.png)
    - 경로 설정
        - ![image](https://user-images.githubusercontent.com/90626691/179381583-d8ba2720-2f24-4ee7-aafd-6007e3639c84.png)
    - 경로 허용
        - SecurityConfiguration

        ```java
        @Override
        public void configure(WebSecurity web) throws Exception {
            web.ignoring().antMatchers("/favicon.ico", "/files/**");
            super.configure(web);
        }
        ```


### 마지막 로그인 날짜

- member/entity/member.java
    - 마지막 로그인 날짜 컬럼 추가 (`rctLoginDt`)
- `AuthenticationSuccessHandler` 만들어서 로그인 성공했을 때 현재 시각을 member 테이블에 저장
- MemberDto에도 로그인 날짜 정보 추가
    - 포맷팅(`getRctLoginDtText`)
- admin/member/list.html 
    - 마지막 로그인 날짜 컬럼 추가

- [코드](https://github.com/eongiin/fastlms/commit/3d0141d6af5d1582033d2200badc96cfde975e5f){:target="_blank"}


### 사용자별 히스토리 목록

- RequestUtils
    - 접속 IP 정보 가져오기

        ```java
        public static String getClientIP(HttpServletRequest request) {
            String ip = request.getHeader("X-FORWARDED-FOR");
            if (ip == null) {
                ip = request.getHeader("Proxy-Client-IP");
            }
            if (ip == null) {
                ip = request.getHeader("WL-Proxy-Client-IP");
            }
            if (ip == null) {
                ip = request.getHeader("HTTP_CLIENT_IP");
            }
            if (ip == null) {
                ip = request.getHeader("HTTP_X_FORWARDED_FOR");
            }
            if (ip == null) {
                ip = request.getRemoteAddr();
            }
            return ip;
        }
        ```
    - 접속 UserAgent 정보 가져오기

        ```java
        public static String getUserAgent(HttpServletRequest request) {
            return request.getHeader("User-Agent");
        }
        ```
- 로그인 히스토리 테이블 생성
    - 로그인 아이디, 로그인 날짜, 접속 IP, 접속 UserAgent
    - Member와 연결 (`@ManyToOne`)
- 로그인 성공할 때마다 히스토리 정보 저장
    - LoginHistoryRepository.findByUserIdOrOrderByIdDesc(userId)
 - LoginHistoryDto 생성
    - of
- LoginHistory 테이블에서 필요한 정보 가져오기
    - LoginHistoryService.list(userId)
    - AdminMemberController에서 detail 페이지로 갈 때 로그인 히스토리 정보 가져옴
- 회원 관리 - 사용자 상세 정보 아래 로그인 접속 목록 표시
- [코드](https://github.com/eongiin/fastlms/commit/197030a103edf653aa1c3a73aaf3e2338f9fcdee){:target="_blank"}

### 배너관리 (admin)
- 배너 테이블 생성
    - 배너 ID
    - 배너명
    - 배너 이미지
        - original
        - url
    - 대체 텍스트 (alt)
    - 링크 주소
    - 오픈 방법
        - Option (Enum)
            - 새 창
            - 현재 창
    - 정렬 순서
    - 공개 여부
    - 등록일

- BannerDto
    - 페이징 처리 위해 totalCount, seq 필요
    - 날짜 포맷팅
    - 화면에 보여줄 정보
- BannerParam
    - 페이징 처리
    - 추가, 수정할 BannerId 
- BannerInput
    - 사용자로부터 입력 받은 추가, 수정할 정보 

- [코드](https://github.com/eongiin/fastlms/commit/4a038b7c5ddd0775a6c62a6bdfdb361bbc44cc52){:target="_blank"}

### 프론트 배너 노출


## 🔔 Tomorrow

- LMS프로젝트 마무리
    - 프론테 배너 노출
- 개인 프로젝트 주제 정하기
    - 사용하고 싶은 기술