---
layout: post
title: June 1st 2022
subtitle: 2022.05.30~06.05
# description: >
#   2022.06. 첫째 주 알게 된 내용
date: '2022-05-30 0:0:0'
categories:
  - til
tags: [db,java]
related_posts:
  - 
sitemap: false
published: true
---
# [ 22.06.1st ] : DB(SQL, ERD, JDBC)

* toc 
{:toc}

# 30 Mon

제로베이스 백엔드 스쿨 박규태 강사님 데이터베이스/자바 프로젝트 강의를 들으며 정리한 글입니다  

## ✏️ DB

### 영화 데이터 실습

- 데이터 출처 : [KOBIS](https://www.kobis.or.kr/kobis/business/mast/mvie/searchMovieList.do){:target="_blank"}

#### 테이블 정보

```sql
CREATE TABLE `actor` (
  `code` varchar(10) NOT NULL,
  `name` varchar(100) DEFAULT NULL COMMENT '이름',
  `eng_name` varchar(100) DEFAULT NULL COMMENT '영문명',
  `birth` varchar(10) DEFAULT NULL COMMENT '생년월일',
  `country` varchar(100) DEFAULT NULL COMMENT '국가명',
  `domain` varchar(50) DEFAULT NULL COMMENT '도메인',
  `pilmo` varchar(255) DEFAULT NULL COMMENT '필모',
  `reg_dt` datetime DEFAULT current_timestamp(),
  PRIMARY KEY (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

```sql
CREATE TABLE `company` (
  `code` varchar(10) NOT NULL,
  `company_name` varchar(100) DEFAULT NULL,
  `eng_company_name` varchar(100) DEFAULT NULL,
  `category` varchar(600) DEFAULT NULL,
  `ceo` varchar(50) DEFAULT NULL,
  `phone` varchar(30) DEFAULT NULL,
  `homepage` varchar(100) DEFAULT NULL,
  `addr` varchar(255) DEFAULT NULL,
  `pilmo` varchar(4000) DEFAULT NULL,
  PRIMARY KEY (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

```sql
CREATE TABLE `festival` (
  `code` varchar(255) NOT NULL,
  `title` varchar(100) DEFAULT NULL,
  `eng_title` varchar(100) DEFAULT NULL,
  `continent` varchar(50) DEFAULT NULL,
  `country` varchar(50) DEFAULT NULL,
  `city` varchar(50) DEFAULT NULL,
  `gerne` varchar(30) DEFAULT NULL,
  `important_flag` varchar(20) DEFAULT NULL,
  `homepage` varchar(255) DEFAULT NULL,
  `first_open_date` varchar(20) DEFAULT NULL,
  PRIMARY KEY (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

```sql
CREATE TABLE `movie` (
  `code` varchar(10) NOT NULL,
  `title` varchar(255) DEFAULT NULL,
  `eng_title` varchar(255) DEFAULT NULL,
  `pub_year` int(11) DEFAULT NULL,
  `country` varchar(50) DEFAULT NULL,
  `movie_type` varchar(20) DEFAULT NULL,
  `genre` varchar(50) DEFAULT NULL,
  `open_flag` varchar(20) DEFAULT NULL,
  `director` varchar(1000) DEFAULT NULL,
  `production` varchar(1000) DEFAULT NULL,
  PRIMARY KEY (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

```sql
CREATE TABLE `screen` (
  `code` varchar(10) NOT NULL,
  `sido` varchar(20) DEFAULT NULL,
  `gugun` varchar(50) DEFAULT NULL,
  `screen_name` varchar(100) DEFAULT NULL,
  `screen_count` int(11) DEFAULT NULL,
  `seat_count` int(11) DEFAULT NULL,
  `open_type` varchar(10) DEFAULT NULL,
  `reg_type` varchar(10) DEFAULT NULL,
  `biz_name` varchar(100) DEFAULT NULL,
  `open_date` date DEFAULT NULL,
  `open_status` varchar(10) DEFAULT NULL,
  PRIMARY KEY (`code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```
#### SQL 문제

- 실제로 주어진 데이터 내용을 보면 값이 없는 경우 `NULL`이 아닌 공백이 들어가 있어서 이 부분 추가로 처리해 줬다

<b>(1) 영화 정보중 영문 제목이 없는 데이터를 조회하는 SQL문을 작성해 보세요.</b>  
[관련내용] NULL 함수, TRIM 함수, LEN 함수

```sql
select
	*
from
	movie
where
	eng_title is null
	or trim(eng_title)= '';
-- where length(trim(eng_title))=0;
```

<b>(2) 한국 영화중 2001년도에 개봉한 영화 중 액션 영화를 조회하는 SQL문을 작성해 보세요.</b>  
[관련내용] LIKE 조건

```sql
select
	*
from
	movie
where
	country = '한국'
	and pub_year = 2001
	and genre like '액션%';
```
(테이블에 들어가있는 장르 데이터가 장르 뒤에 숫자가 붙어 나오는 경우도 있어서 (ex - `액션(2)`)   
`genre = '액션'` 대신 `like`를 이용하여 찾았다)


<b>(3) 싸이더스가 2020년도에 개봉한 영화 감독의 출생년도를 조회하는 SQL를 작성해 보세요.</b>  
[관련내용] IN 조건

```sql
select
	birth
from
	actor
where
	domain = '감독'
	and name in (
	select
		director
	from
		movie
	where
		production like '%싸이더스%'
		and pub_year = 2020);
```

<b>(4) 영화인 정보에서 직업을 중복없이 조회하는 SQL문을 작성해 보세요.</b>  
[관련내용] DISTINCT 이용, NULL 체크

```sql
select
	distinct domain
from
	actor
where
	domain is not null
	and trim(domain) <> '';
```

- group by로도 조회할 수 있다

```sql
select
	domain
from
	actor
where
	domain is not null
	and trim(domain) <>  ''
group by
	domain;
```

<b>(5) 영화 감독의 국가가 독일이고 2020년 이후에 개봉된 영화의 제목, 감독, 개봉일자, 장르를 최근 개봉일자 순으로 조회하는 SQL문을 작성해 보세요.</b>  
[관련내용] JOIN, COLUMN ALIAS, TABLE ALIAS, 순서 정렬


```sql
select
	m.title,
	m.director,
	m.pub_year,
	m.genre
from
	movie m
join actor a on
	(m.director = a.name
		and a.domain = '감독')
where
	m.pub_year >= 2020
	and a.country = '독일'
order by
	m.pub_year desc;
```

<b>(6) 시카고에서 진행하는 축제중에서 영문 제목이 없는 경우 한글 제목으로 보여주며, 장르가 없는 경우
`기타`로 표시하며, 홈페이지가 없는 경우 `홈페이지 없음` 으로 조회하는 SQL문을 작성해 보세요.</b>  
[관련내용] CASE 문

```sql
select
	code ,
	title ,
	case
		when ifnull(eng_title , '') = '' then title
		else eng_title
	end as eng_title ,
	country ,
	city ,
	case
		when ifnull(gerne, '') = '' then '기타'
		else gerne
	end as gerne ,
	important_flag ,
	case 
		when ifnull(homepage, '') = '' then '홈페이지 없음'
		else homepage
	end as homepage,
	first_open_date
from
	festival
where
	country = '미국'
	and city = '시카고';
```

<b>(7) 영화 상영관 회사별로 좌석수가 가장 많은 값을 구하고 이를 좌석수가 많은 순서대로 조회하는 SQL문을 작성해
보세요.  
(추가 : 순위 매기기)</b>    
[관련내용] row_number(), max() 함수, group by, order by


```sql
select
	row_number() over (
	order by max(seat_count) desc) as ranking,
	biz_name,
	max(seat_count) as max_seat_count
from
	screen
where
	biz_name is not null
	and trim(biz_name) <> ''
group by
	biz_name
order by
	max_seat_count desc;
```

<b>(8) 국가별 영화 정보의 개수를 조회하는 SQL 구문을 작성해 보세요.  
(단, 국가가 없는 경우는 ‘국가 미상' 으로 처리해 주세요.)</b>  
[관련내용] group by, order by, trim() 함수, count() 함수, case 문


```sql
select 
	case
		when ifnull(country, '') = '' then '국가 미상'
		else country
	end as country ,
	count(*) as movie_count
from
	movie
group by
	country
order by
	movie_count desc;
```

<b>(9) CGV 극장 중에서 스크린 수가 가장 많은 극장의 순위를 5위까지 조회하는 SQL 문을 작성해 보세요.</b>  
[관련내용] order by, row_number, limit

```sql
select
	row_number() over(
	order by screen_count desc) as ranking,
	s.*
from
	screen s
where
	screen_name like 'CGV%'
order by
	screen_count desc
limit 5;
```

<b>(10) 스크린 수가 가장 많은 극장의 이름을 조회하는 SQL문을 작성해 보세요.</b>

```sql
select
	*
from
	screen
where
	screen_count in (
	select
		max(screen_count)
	from
		screen);
```

<b>(11) 전국 극장의 스크린 수의 평균과 ‘서울시'에 위치한 극장의 스크린 수의 평균을 구하고, 이
둘의 차이를 조회하는 SQL문을 작성해 보세요.  
(단, 최종 차이의 평균은 ~~소수점 첫째 자리에서 반올림하여 구합니다.~~ → 반올림하여 소수 첫째 자리까지 나타내기)</b>  
[관련내용] round() 함수, avg() 함수, sub query

```sql
select
	*,
	round(abs(screen_count_all_avg - screen_count_sido_avg), 1) as avg_diff
from
	(
	select
		(
		select
			avg(screen_count)
		from
			screen) as screen_count_all_avg,
		(
		select
			avg(screen_count)
		from
			screen
		where
			sido = '서울시') as screen_count_sido_avg) T1;
```

<b>(12) 한국에서만 하는 축제 중에서 도시별로 진행하는 축제가 10개 이상인 도시를 가장 많이
진행하는 순서로 출력하는 SQL문을 작성해 보세요.  
(단, 도시정보가 없는 경우는 제외해 주세요.)</b>   
[관련내용] group by, having

```sql
select
	city,
	count(*) as festival_count
from
	festival
where
	country = '한국'
	and city is not null
	and trim(city) <> ''
group by
	city
having
	festival_count >= 10
order by
	festival_count desc;
```

<b>(13) 영화인 정보에서 직업이 배우가 아닌 사람 중에서 1980년 ~ 1990년 사이에 출생자를 출생년도 순으로 조회하는 SQL문을 작성해 주세요.  
(생년월일 정보가 유효하지 않은 사람은 제외해 주세요.)</b>  
[관련내용] in, is not null, length() 함수, trim() 함수, str_to_date()함수, between a and b

```sql
select
	*
from
	actor
where
	domain <> '배우'
	and birth is not null
	and length(trim(birth)) = 10
	and str_to_date(birth, '%Y-%m-%d') is not null
	and year(str_to_date(birth, '%Y-%m-%d')) between 1980 and 1990
order by
	str_to_date(birth, '%Y-%m-%d');
```

<b>(14) 헝가리 출신의 영화인중 직업이 배우인 사람의 출생년도, 월, 일자를 각각 출력하는 SQL문을 작성해 보세요.  
(단, 생일이 입력되지 않은 사람은 제외해 주세요.)</b>  
[관련내용] str_to_date, year()함수, month()함수, day()함수, length()함수, trim()함수, is not null

```sql
select
	year(str_to_date(birth, '%Y-%m-%d')) as birth_year,
	month (str_to_date(birth, '%Y-%m-%d')) as birth_month,
	day(str_to_date(birth, '%Y-%m-%d')) as birth_day
from
	actor
where
	country = '헝가리'
	and domain = '배우'
	and birth is not null
	and length(trim(birth)) = 10
	and str_to_date(birth, '%Y-%m-%d') is not null;
```

<b>(15) 서울시에 위치한 극장 중에서 강남구가 아닌 극장의 좌석 수의 합을 조회하는 SQL문을 작성해 보세요.  
(좌석 수는 천단위로 콤마(,)로 표시해 주세요.)</b>  
[관련내용] format()함수, sum()함수, not in

```sql
select
	format(sum(seat_count), 0) as total_sum
from
	screen s
where
	sido = '서울시'
	and gugun <> '강남구';
```

<b>(16) 전국의 CGV 극장의 이름과 규모를 조회하되 규모는 스크린 수가 5보다 작으면 '소', 5보다
크거나 같고 10보 작으면 '중', 10보다 크거나 같으면 '대'로 표시하는 SQL문을 작성해 주세요.</b>  
[관련내용] case 문, like

```sql
select
	screen_name ,
	screen_count ,
	case
		when screen_count < 5 then '소'
		when screen_count < 10 then '중'
		else '대'
	end as screen_scale
from
	screen
where
	screen_name like 'CGV%';
```

<b>(17) 출신지가 프랑스나 이탈리아 이면서 직업이 촬영이거나 편집인 영화인을 조회하는
SQL문을 작성해 주세요.</b>  
[관련내용] and 조건, or 조건, in 조건

```sql
select
	*
from
	actor
where
	country in ('프랑스', '이탈리아')
	and domain in ('촬영', '편집');
```

<b>(18) 2010년 ~ 2020년 기간중 연도별로 상영된 영화의 수를 조회하는 SQL문을 작성해 주세요.</b>  
[관련내용] count() 함수, between a and b 구문

```sql
select
	pub_year,
	count(*) as pub_year_count
from
	movie
where
	pub_year between 2010 and 2020
group by
	pub_year;
```

<b>(19) 한국인 중에서 직업이 배우인 사람의 이름을 조회하되, 개인정보 보호를 위해서 이름
중간에 '`*` 처리를 통하여 이름이 전체적으로 노출되지 않도록 처리해 주세요.  
(단, 이름은 적어도 2개 이상의 문자이고 외자인 경우 성만 표시하며 4자인 경우는 성과 맨 마지막만
보여주며, 그 이상인 이름의 경우는 첫 성만 보여주고 나머지는 마스크(`*`) 처리해 주세요.)</b>  
[관련내용] length()함수, trim()함수, char_length()함수, rpad()함수, substring()함수

```sql
select
	name,
	case
		when char_length(trim(name)) = 2 then concat(substring(trim(name), 1, 1), '*')
		when char_length(trim(name)) = 3 then concat(substring(trim(name), 1, 1), '*' , substr(trim(name), 3))
		when char_length(trim(name)) = 4 then concat(substring(trim(name), 1, 1), '**' , substring(trim(name), 4))
		else rpad(substring(trim(name), 1, 1), char_length(trim(name)), '*')
	end as name_mask
from
	actor
where
	country = '한국'
	and domain = '배우'
	and char_length(trim(name)) > 1;
```

<b>(20) 한국인 이면서 '배'씨 성을 가진 사람 중에 '준'으로 끝나는 이름을 가진 영화인을 조회하는
SQL문을 작성해 주세요.  
(이름이 3글자인 경우만)</b>  
[관련내용] like 문, 와일드카드 _

```sql
select
	*
from
	actor
where
	country = '한국'
	and name like '배_준';
```

<b>(21) 한국인 이면서 '원'씨 성을 가진 배우를 조회하는 SQL문을 작성해 주세요.</b>  
[관련내용] like 문, 와일드카드 %

```sql
select
	*
from
	actor
where
	country = '한국'
	and domain = '배우'
	and name like '원%';
```

<b>(22) 대전시에 있는 상영관 중에서 좌석 수가 500 ~ 1000 사이인 극장의 이름을 조회하는
SQL문을 작성해 주세요.</b>  
[관련내용] between a and b 구문

```sql
select
	*
from
	screen
where
	sido = '대전시'
	and seat_count between 500 and 1000;
```

<b>(23) 시도별 상영관의 전체 좌석 수, 최대 좌석 수, 최소 좌석 수, 평균 좌석 수 내용을 조회하는
SQL문을 작성해 주세요.  
(단, 좌석 수가 0인 상영관은 제외하고, 평균 좌석 수는 소수점 올림으로 처리해 주세요.)</b>  
[관련내용] sum ()함수, max() 함수, min() 함수, avg() 함수, ceil() 함수, group by 구문

```sql
select
	sido,
	sum(seat_count) as seat_count_sum,
	max(seat_count) as seat_count_max,
	min(seat_count) as seat_count_min,
	ceil(avg(seat_count)) as seat_count_avg
from
	screen
where
	seat_count > 0
group by
	sido;
```

<b>(24) 시도별 평균 좌석수가 900 이상인 상영관을 구하는 SQL문을 작성해 주세요.  
(단, 시도별 평균 좌석 수는 소수점 2자리까지 반올림해서 표현해 주세요.)  
(추가 - 평균 좌석수가 높은순으로 정렬)</b>  
[관련내용] round() 함수, avg() 함수, group by 구문, having 구문

```sql
select
	screen_name,
	round(avg(seat_count), 2) as seat_count_avg
from
	screen
group by
	sido
having
	seat_count_avg >= 900
order by
	seat_count_avg desc;
```

<b>(25) 영화인 중에서 생일을 알고 있고, 3월에 태어난 사람 중 직업이 감독인 사람의 한글이름, 
영문이름을 조회하여, 출신국가는 한국이면, '국내', 한국이 아니면 '국외'로 표시하는 SQL문을 작성해 주세요.</b>  
[관련내용] case 문, length() 함수, trim() 함수, str_to_date() 함수

```sql
select
	name,
	eng_name,
	case
		when country = '한국' then '국내'
		else '국외'
	end as country_comment
from
	actor
where
	domain = '감독'
	and length(trim(birth)) = 10
	and str_to_date(birth, '%Y-%m-%d') is not null
	and month(str_to_date(birth, '%Y-%m-%d')) = 3;
```

<b>(26) 제주도에 위치한 상영관을 좌석수가 많은 순서로 정렬하는 SQL문을 작성해 보세요.</b>  
[관련내용] order by 문

```sql
select
	*
from
	screen
where
	sido = '제주도'
order by
	seat_count desc;
```

<b>💫 (27) 한국에서 개최되는 주요 축제가 개최된 도시의 상영관 수를 각각 조회하는 SQL문을 작성해
주세요. </b>  
[관련내용] ifnull() 함수, sum() 함수, concat() 함수, left join 문, group by 문, 서브 쿼리 문


```sql
select
	f2.*,
	ifnull(s.screen_count_sum, 0) as screen_count_sum
from
	(
	select
		concat(city, '시') as city
	from
		festival
	where
		country = '한국'
		and important_flag = '예'
) f2
left join (
	select
		sido,
		sum(screen_count) as screen_count_sum
	from
		screen s
	group by
		sido
) s on
	s.sido = f2.city;
```

<b>(28) 새로운 축제를 등록하기 위해서 code값의 최대값에 + 1을 더한 신규 코드값을 가져오는 SQL 
문을 작성해 보세요.</b>  
[관련내용] max(), ifnull()

```sql
select
	ifnull(max(code), 0)+ 1
from
	festival;
```

<b>(29) 한국 배우중에서 이름과 영문이름이 존재하고, 생년월일 데이터가 정확한 배우의 전체
개수와 이름 순으로 10개 단위로 페이징 처리하기 위한 SQL문을 작성해 주세요.</b>  
[관련내용] count() 함수, trim() 함수, length() 함수, str_to_date() 함수, row_number() 함수, order
by 구문, limit 구문

- limit 이용 

```sql
select
	row_number() over(
	order by name) as ranking,
	a.*
from
	actor a
where
	country = '한국'
	and domain = '배우'
	and length(trim(birth)) = 10
	and str_to_date(birth, '%Y-%m-%d') is not null
	and name is not null
	and trim(name) <> ''
	and eng_name is not null
	and trim(eng_name) <> ''
order by
	name
limit 30, 10;
```

- 서브쿼리 이용

```sql
select
	t2.*
from
	(
	select
		t1.*
	from
		(
		select
			row_number() over(
		order by
			name) as ranking,
			a.*
		from
			actor a
		where
			country = '한국'
			and domain = '배우'
			and length(trim(birth)) = 10
			and str_to_date(birth, '%Y-%m-%d') is not null
			and name is not null
			and trim(name) <> ''
			and eng_name is not null
			and trim(eng_name) <> ''
		order by
			name	
) t1
	where
		t1.ranking <= 40) t2
where
	t2.ranking > 30;
```

<b>(30) 영화인이 속한 국가와 축제가 진행되는 국가 모두를 표시하는 SQL문을 작성해 주세요.</b>  
[관련내용] distinct, union, is not null 구문, trim() 함수

```sql
select
	distinct country
from
	actor
where
	country is not null
	and trim(country) <> ''
union
select
	distinct country
from
	festival
where
	country is not null
	and trim(country) <> '';
```

<b>(31) 한국의 감독 중에서 이름, 영문이름이 모두 있고, 생년월일이 정확하며 1990년에 출생한
감독들의 작품중에서 한국에서 개봉한 영화제목을 보여주는 SQL문을 작성해 보세요.  
(영화가 여러개인 경우 콤마(,)를 이용해서 한 컬럼에 표시해 주세요.)</b>  
[관련내용] group_concat() 함수, is not null 구문, trim() 함수, length() 함수, str_to_date() 함수, 
year() 함수, order by 구문

- WHERE절 서브 쿼리

```sql
select
	m.director,
	group_concat(m.title) as movie_title
from
	movie m
where
	country = '한국'
	and open_flag = '개봉'
	and m.director in (
	select
		a.name
	from
		actor a
	where
		country = '한국'
		and domain = '감독'
		and name is not null
		and trim(name) <> ''
		and eng_name is not null
		and trim(eng_name) <> ''
		and length(trim(birth)) = 10
		and str_to_date(birth, '%Y-%m-%d') is not null
		and year(str_to_date(birth, '%Y-%m-%d')) = 1990
)
group by
	m.director;
```

-  SELECT절 서브 쿼리
	- `movie_title` 값이 `NULL`인 경우 포함

```sql
select
	a.*,
	(
	select
		group_concat(title)
	from
		movie m
	where
		country = '한국'
		and open_flag = '개봉'
		and m.director = a.name 
) as movie_title
from
	actor a
where
	country = '한국'
	and domain = '감독'
	and name is not null
	and trim(name) <> ''
	and eng_name is not null
	and trim(eng_name) <> ''
	and length(trim(birth)) = 10
	and str_to_date(birth, '%Y-%m-%d') is not null
	and year(str_to_date(birth, '%Y-%m-%d')) = 1990;
```

<b>(32) 영화인 정보중 감독이 '강우석' 이면서 제작한 영화 목록을 년도 순으로 조회하는 SQL문을
작성해 주세요.</b>  
[관련내용] in 구문, order by 구

```sql
select
	m.*
from
	movie m
where
	m.director in (
	select
		a.name
	from
		actor a
	where
		domain = '감독'
		and name = '강우석'
)
order by
	m.pub_year;
```

- 간단하게 그냥 `where m.director = '강우석'`해도 되는 문제긴 했다

### 자바 앱을 통한 DB 핸들링

- READ

1. JDBC 드라이버 로드
	```java
	Class.forName("org.mariadb.jdbc.Driver");
	```
2. DataBase Connection 생성
	```java
	Connection connection = DriverManager.getConnection(url, dbUser, dbPassword);
	```
3. SQL을 위한 Statement 객체 생성
	```java
	PreparedStatement preparedStatement = connection.prepareStatement(sql);
	```
4.  SQL 문장 실행
	```java
	ResultSet resultSet = preparedStatement.executeQuery();
	```
5. SQL 실행 결과 처리
	```java
	while (rs.next()) {
	System.out.println(rs.getString("user_id"));
	}
	```
6. JDBC 객체들 연결 해제
	```java
	resultSet.close();
	preparedStatement.close();
	connection.close();
	```

- CREATE, UPDATE, DELETE

1. JDBC 드라이버 로드
	```java
	Class.forName("org.mariadb.jdbc.Driver");
	```
2. DataBase Connection 생성
	```java
	Connection connection = DriverManager.getConnection(url, dbUser, dbPassword);
	```
3. SQL을 위한 Statement 객체 생성
	```java
	PreparedStatement preparedStatement = connection.prepareStatement(sql);
	```
4.  SQL 문장 실행
	```java
	int affected = preparedStatement.executeUpdate();
	```
5. SQL 실행 결과 처리
	```java
	System.out.printf("affected: %d", affected);
	```
6. JDBC 객체들 연결 해제
	```java
	preparedStatement.close();
	connection.close();
	```

## 🔔 Tomorrow
- Java를 통한 JDBC 프로그램
- ERD를 통한 데이터베이스 설계
- 웹을 통한 데이터베이스 처리 프로그래밍 실습
- 코테 복습

# 31 Tue
## 🌈 DB
### JDBC

#### 실습 준비
- 실습용 ws 설정
	- `D:\dev\zb\실습\java-study`

- mariadb jdk driver 설치
	- `java-study`폴더 아래 `lib`폴더를 만들고 거기에 다운 받은 jar파일을 저장해두겠다

<https://mariadb.com/kb/en/about-mariadb-connector-j/>{:target="_blank"}

![image](https://user-images.githubusercontent.com/90626691/171068172-184afd33-528e-43a2-a6fa-ee92b5366cd3.png)

- intelliJ에서 라이브러리 추가해주기
	- `Project Structure` - `Libraries` - `Java` - 방금 다운 받은 파일 선택

![image](https://user-images.githubusercontent.com/90626691/171068597-c11bfec5-22f8-468d-8d4f-9358b6a00588.png)

![image](https://user-images.githubusercontent.com/90626691/171068776-77e13cc3-fb89-4aa2-8d3d-5bbdad3309a5.png)

#### 연결
- 필요한 정보 5가지 : ip(domain), port, userid, password, instance
	- `jdbc:DB_VENDER://IP_ADDR:IP_PORT/INSTANCE`
		- `jdbc:mariadb://localhost:3306/testdb1`
	- userid : `testuser1`
	- password : 
- 접속 단계
	1. JDBC 드라이버 로드
	2. DataBase Connection 생성
	3. SQL을 위한 Statement 객체 생성
	4. SQL 문장 실행
	5. SQL 실행 결과 처리
	6. JDBC 객체들 연결 해제

##### SELECT

```java
public static void dbSelect(Member member) {
	String url = "jdbc:mariadb://localhost:3306/testdb1";
	String dbUserId = "testuser1";
	String dbPassword = "";

	try {
		Class.forName("org.mariadb.jdbc.Driver");
	} catch (ClassNotFoundException e) {
		e.printStackTrace();
	}

	Connection connection = null;
	PreparedStatement preparedStatement = null;
	ResultSet rs = null;

	try {
		connection = DriverManager.getConnection(url, dbUserId, dbPassword);
		String sql = "select id, password, name" +
				" from member" +
				" where id=?;";

		preparedStatement = connection.prepareStatement(sql);
		preparedStatement.setString(1, member.getUserId());

		rs = preparedStatement.executeQuery();
		while (rs.next()) {
			String userId = rs.getString("id");
			String name = rs.getString("name");
			String password = rs.getString("password");

			System.out.println(userId + ", " + password + ", " + name);
		}

	} catch (SQLException e) {
		e.printStackTrace();
	} finally {
		try {
			if (rs != null && !rs.isClosed()) {
				rs.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
		try {
			if (preparedStatement != null && !preparedStatement.isClosed()) {
				preparedStatement.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
		try {
			if (connection != null && !connection.isClosed()) {
				connection.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
	}
}
```
##### INSERT

```java
public static void dbInsert(Member member) {
	String url = "jdbc:mariadb://localhost:3306/testdb1";
	String dbUserId = "testuser1";
	String dbPassword = "";

	try {
		Class.forName("org.mariadb.jdbc.Driver");
	} catch (ClassNotFoundException e) {
		e.printStackTrace();
	}

	Connection connection = null;
	PreparedStatement preparedStatement = null;

	try {
		connection = DriverManager.getConnection(url, dbUserId, dbPassword);
		String sql = "insert into member(id,name,password) values (?,?,?);";

		preparedStatement = connection.prepareStatement(sql);
		preparedStatement.setString(1, member.getUserId());
		preparedStatement.setString(2, member.getName());
		preparedStatement.setString(3, member.getPassword());

		int affected = preparedStatement.executeUpdate();

		if (affected > 0) {
			System.out.println("저장 성공");
		} else {
			System.out.println("저장 실패");
		}
	} catch (SQLException e) {
		e.printStackTrace();
	} finally {
		try {
			if (preparedStatement != null && !preparedStatement.isClosed()) {
				preparedStatement.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
		try {
			if (connection != null && !connection.isClosed()) {
				connection.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
	}
}
```

##### UPDATE

```java
public static void dbUpdate(Member member) {
	String url = "jdbc:mariadb://localhost:3306/testdb1";
	String dbUserId = "testuser1";
	String dbPassword = "";

	try {
		Class.forName("org.mariadb.jdbc.Driver");
	} catch (ClassNotFoundException e) {
		e.printStackTrace();
	}

	Connection connection = null;
	PreparedStatement preparedStatement = null;

	try {
		connection = DriverManager.getConnection(url, dbUserId, dbPassword);
		String sql = "update member" +
				" set password = ?" +
				" where id = ?;";

		preparedStatement = connection.prepareStatement(sql);
		preparedStatement.setString(1, member.getPassword());
		preparedStatement.setString(2, member.getUserId());

		int affected = preparedStatement.executeUpdate();

		if (affected > 0) {
			System.out.println("수정 성공");
		} else {
			System.out.println("수정 실패");
		}
	} catch (SQLException e) {
		e.printStackTrace();
	} finally {
		try {
			if (preparedStatement != null && !preparedStatement.isClosed()) {
				preparedStatement.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
		try {
			if (connection != null && !connection.isClosed()) {
				connection.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
	}
}
```
##### DELETE

```java
public static void dbDelete(Member member) {
	String url = "jdbc:mariadb://localhost:3306/testdb1";
	String dbUserId = "testuser1";
	String dbPassword = "";

	try {
		Class.forName("org.mariadb.jdbc.Driver");
	} catch (ClassNotFoundException e) {
		e.printStackTrace();
	}

	Connection connection = null;
	PreparedStatement preparedStatement = null;
	try {
		connection = DriverManager.getConnection(url, dbUserId, dbPassword);
		String sql = "delete from member" +
				" where id = ?;";

		preparedStatement = connection.prepareStatement(sql);
		preparedStatement.setString(1, member.getUserId());

		int affected = preparedStatement.executeUpdate();

		if (affected > 0) {
			System.out.println("삭제 성공");
		} else {
			System.out.println("삭제 실패");
		}
	} catch (SQLException e) {
		e.printStackTrace();
	} finally {
		try {
			if (preparedStatement != null && !preparedStatement.isClosed()) {
				preparedStatement.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
		try {
			if (connection != null && !connection.isClosed()) {
				connection.close();
			}
		} catch (SQLException e) {
			e.printStackTrace();
		}
	}
}
```
### ERD를 통한 데이터베이스 설계
#### ExERD 설치

- <https://ko.exerd.com/down.jsp>{:target="_blank"}
- 이클립스 플러그인으로 설치할 것이기 때문에 먼저 이클립스 설정
	- ws 설정(나는 `D:\dev\zb\실습\ec-ws`) 
	- 인코딩 설정
		- `Windows` - `Preference` - `encoding` 검색
		- `Workspace`, `CSS Files`, `HTML Files`, `JSP Files` 모두 `UTF-8`로 설정
- 위 주소로 들어가서 이클립스 플러그인으로 설치 아래 다운로드를 누르고 3.X ~ 버전 주소를 누른 후 그 주소를 복사하고 이클립스로 돌아옴
- `Help` - `Install New Software` - `Add` - 주소 복사 붙여넣기 
- 생성된 `exERD`를 체크하고 `Next` 클릭해서 설치
- Select All - Trust selected
- eclipse restart

![image](https://user-images.githubusercontent.com/90626691/171099181-799f9f66-455c-42c8-a1e2-4fd0687b0ed9.png)

![image](https://user-images.githubusercontent.com/90626691/171099509-00ca059f-76a0-4c45-9ad4-757e8da70cfa.png)

![image](https://user-images.githubusercontent.com/90626691/171099627-84835694-53ba-46cb-8d38-fb18fa00190d.png)

![image](https://user-images.githubusercontent.com/90626691/171099838-3202c57b-7ef0-42ca-b218-1d432c555e71.png)


#### 쇼핑몰 주문정보 모델링

- 프로젝트 생성 (`General` - `Project` : `erd-study`)
- `exERD File` 생성
	- `MySQL` 선택하고 이름을 `shop`으로 설정

![image](https://user-images.githubusercontent.com/90626691/171101147-c174131c-8b79-42ad-864a-dc2a96da2b65.png)

- 환경에 맞는 `Perspective` 설정

![image](https://user-images.githubusercontent.com/90626691/171101588-0fef8ad7-bc93-42a1-b429-402fa4f7946d.png)

- ER-Diagram 작성
	- 컬럼 추가
		- 기본키: Ctrl + Shift + Enter
		- 일반컬럼: Ctrl + Enter


	![image](https://user-images.githubusercontent.com/90626691/171130468-5c1691c1-934f-46a4-aac5-c0f4fd589d1d.png)

	- (1) 테이블 생성
	- (2) 비식별 관계 생성
	- (3) 테이블 헤더 보이기
	- (4) 논리 모드 / 물리 모드 변환
	- (5) 화살표 눌러서 논리/물리 동시 편집
- SQL문 생성
	- `eXERD` - `포워드 엔지니어링`

```sql
-- 회원
CREATE TABLE `MEMBER` (
	`USER_ID` VARCHAR(50)  NOT NULL COMMENT '아이디', -- 아이디
	`NAME`    VARCHAR(20)  NULL     COMMENT '이름', -- 이름
	`PHONE`   VARCHAR(12)  NULL     COMMENT '연락처', -- 연락처
	`PWD`     VARCHAR(255) NULL     COMMENT '비밀번호', -- 비밀번호
	`EMAIL`   VARCHAR(50)  NULL     COMMENT '이메일', -- 이메일
	`REG_DT`  DATE         NULL     COMMENT '가입일시' -- 가입일시
)
COMMENT '회원';

-- 회원
ALTER TABLE `MEMBER`
	ADD CONSTRAINT `PK_MEMBER` -- 회원 기본키
		PRIMARY KEY (
			`USER_ID` -- 아이디
		);

-- 상품
CREATE TABLE `ITEM` (
	`ITEM_ID`   INT          NOT NULL COMMENT '상품키', -- 상품키
	`ITEM_NAME` VARCHAR(100) NULL     COMMENT '상품명', -- 상품명
	`COMMENT`   TEXT         NULL     COMMENT '설명', -- 설명
	`PRICE`     INT          NULL     COMMENT '가격', -- 가격
	`STOCK_YN`  BIT          NULL     COMMENT '재고여부' -- 재고여부
)
COMMENT '상품';

-- 상품
ALTER TABLE `ITEM`
	ADD CONSTRAINT `PK_ITEM` -- 상품 기본키
		PRIMARY KEY (
			`ITEM_ID` -- 상품키
		);

ALTER TABLE `ITEM`
	MODIFY COLUMN `ITEM_ID` INT NOT NULL AUTO_INCREMENT COMMENT '상품키';

-- 주문
CREATE TABLE `ITEM_ORDER` (
	`ORDER_ID` INT         NOT NULL COMMENT '주문키', -- 주문키
	`AMOUNT`   INT         NULL     COMMENT '주문수량', -- 주문수량
	`ORDER_DT` DATE        NULL     COMMENT '주문일시', -- 주문일시
	`PAY`      INT         NULL     COMMENT '결제금액', -- 결제금액
	`USER_ID`  VARCHAR(50) NULL     COMMENT '아이디', -- 아이디
	`ITEM_ID`  INT         NULL     COMMENT '상품키' -- 상품키
)
COMMENT '주문';

-- 주문
ALTER TABLE `ITEM_ORDER`
	ADD CONSTRAINT `PK_ITEM_ORDER` -- 주문 기본키
		PRIMARY KEY (
			`ORDER_ID` -- 주문키
		);

ALTER TABLE `ITEM_ORDER`
	MODIFY COLUMN `ORDER_ID` INT NOT NULL AUTO_INCREMENT COMMENT '주문키';

-- 주문
ALTER TABLE `ITEM_ORDER`
	ADD CONSTRAINT `FK_ITEM_TO_ITEM_ORDER` -- 상품 -> 주문
		FOREIGN KEY (
			`ITEM_ID` -- 상품키
		)
		REFERENCES `ITEM` ( -- 상품
			`ITEM_ID` -- 상품키
		);

-- 주문
ALTER TABLE `ITEM_ORDER`
	ADD CONSTRAINT `FK_MEMBER_TO_ITEM_ORDER` -- 회원 -> 주문
		FOREIGN KEY (
			`USER_ID` -- 아이디
		)
		REFERENCES `MEMBER` ( -- 회원
			`USER_ID` -- 아이디
		);
```

- `shop` DB를 만들고 계정을 하나 만들어 권한을 주고 거기에 위의 SQL 문을 복붙하여 테이블을 생성했다

```sql
create database shop;
use mysql;
create user 'shop_user' @'localhost' identified by 'shop1234';
grant all privileges on shop.* to 'shop_user' @'localhost' identified by 'shop1234';

flush privileges;
```

### 웹을 통한 데이터베이스 처리 프로그래밍

#### Tomcat 설치
- <https://tomcat.apache.org/download-90.cgi>{:target="_blank"}
- `Tomcat 9` - `64-bit-Windows.zip`
- `D:\dev`에 저장해 뒀음
- 한 번 실행해 보기
	- `D:\dev\apache-tomcat-9.0.63\bin` 여기서 CMD 창 열기
	- `startup.bat`
	- `JAVA_HOME` 설정을 안 해놨다면 설정해 주기
		- 내 PC - 속성 - 고급 시스템 설정 - 환경 변수
		- 시스템 변수
			-  `JAVA_HOME` → `C:\Program Files\Java\jdk1.8.0_321`
			- `PATH` → `%JAVA_HOME%\bin` 추가
- 한글 깨졌지만 아래처럼 실행은 된다

	![image](https://user-images.githubusercontent.com/90626691/171187034-eb4041f7-2a13-44ec-a6a4-d14e751ce6c6.png)

- `http://localhost:8080/`로 접속하여 확인
	- 내용 바꿔보기
		- `D:\dev\apache-tomcat-9.0.63\webapps\ROOT\index.jsp` 열기

		 ![image](https://user-images.githubusercontent.com/90626691/171187930-ff1808ea-f0d2-4aff-be19-e096af81a98a.png)
		- 이렇게 문구 추가

		![image](https://user-images.githubusercontent.com/90626691/171188067-b6dd3a22-5e6e-4075-a902-1e3758b2f851.png)
		- 잘 바뀌었다

#### Eclipse

- 플러그인 설치
	- `Help` - `Eclipse Marketplace`
	- 내가 설치한 것
		- `Spring Tools 4`
		- `Darkest Dark Theme`
		- `Eclipse Enterprise Java and Web Developer Tools`

- 프로젝트 생성
	- `Dynamic Web Project` ( 프로젝트명 : `jsp-study`)
	- `webapp` 아래 `html` 파일 생성 (HTML5, 파일명 : `index.html`)
	- `src/main/java` 아래에 `java` 파일 생성 (파일명 : `Hello`)
- 아파치 톰캣 서버 설정
	- `jsp-study` - `Run on Server` - `Apache/Tomcat v9.0 Server`

	![image](https://user-images.githubusercontent.com/90626691/171201641-63ec50ae-1758-4142-8a92-2203c9cfb083.png)
	- 설정 완료하면 `http://localhost:8080/jsp-study/` 화면이 나온다 (index.html 화면)
- 동적 파일 생성
	- `hello.jsp` 생성
	- `http://localhost:8080/jsp-study/hello.jsp`
	- jsp 파일이 실행될 때 java파일로 바뀐 후 class 파일로 컴파일

```jsp
<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<%
	System.out.println("Hello JSP!");
	out.write("Hello");
	%>
</body>
</html>
```


#### 회원관리 프로그램

다음에 이어서..


## 🔔 Tomorrow
- 다이나믹 프로그래밍 문제
- 코테 확인

# 01 Wed

# 02 Thu

# 03 Fri

# 04 Sat

# 05 Sun