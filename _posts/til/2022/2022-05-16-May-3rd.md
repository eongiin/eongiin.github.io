---
layout: post
title: May 3rd 2022
subtitle: 2022.05.16~05.22
# description: >
#   2022.05. 셋째 주 알게 된 내용
date: '2022-05-16 0:0:0'
categories:
  - til
tags: [aws,db,java,two-pointers]
related_posts:
  - _posts/problem-solving/problem/2022-05-17-pb009.md
  - _posts/problem-solving/problem/2022-05-18-pb010.md
  - _posts/problem-solving/problem/2022-05-19-pb011.md
  - _posts/problem-solving/problem/2022-05-19-pb012.md
sitemap: false
published: true
---
# [ 22.05.3rd ] : DB, 투 포인터, AWS, PS

* toc 
{:toc}

# 16 Mon

## 🌈 DB
- 데이터베이스에 루트로 접속
  - `mysql -u root -p` (비밀번호 입력)
- 데이터베이스 목록 확인
  - `SHOW DATABASES;`
- 데이터베이스 인스턴스 생성과 삭제
  - `CREATE DATABASE DB명;`
  - `DROP DATABASE DB명;`
- 데이터베이스 선택
  - `USE DB명;`
- 테이블 목록 확인
  - `SHOW TABLES;`
- 사용중인 계정 조회
  - `SELECT host, user, password FROM user;`
- 계정 생성
  - `CREATE USER 'username'@'%' IDENTIFIED BY 'password';`
  - `'id'@'localhost'`로 생성하면 로컬 호스트에서만 접속 가능
  - `%`는 외부 접근 모두 허용
- 권한 부여
  - `GRANT ALL PRIVILEGES ON DB명.* TO 'username'@'%' IDENTIFIED BY 'password';`
- 새로고침
  - `FLUSH PRIVILEGES;`
- 계정 삭제
  - `DROP USER 'username'@'host';`
- DBeaver로 접속  
  - (DB - testdb1, user - testuser1)
  - host, port, database, username, password

![image](https://user-images.githubusercontent.com/90626691/168601435-783154ef-7243-4106-ab5a-c8ac0cb47abf.png)

- 자주 쓰는 자료형 
  - `INT`, `DOUBLE`, `CHAR`, `VARCHAR`, `BIT`, `DATETIME`, `TIMESTAMP`, `TEXT`
- 테이블 생성

```sql
create table member(
	id varchar(50),
	name varchar(20),
	password varchar(20),
	old_year int,
	birth_date date,
	sms_send_yn bit,
	register_date datetime
);
```

- ~~ec2 RDS intelliJ 연결 주말에 해보기~~
→ <a href="#rds">이동</a>

## 🔔 Tomorrow

- 투 포인터
- 그리디

# 17 Tue

## 🌻 PS
오늘 푼 그리디 관련 백준 문제는 <a href="#rps">Related Posts</a> 확인

### 투 포인터

- 문자열 s 거꾸로 출력하기 (단, 각 단어의 알파벳 순서는 그대로)
  - ex) `red orange yellow` → `yellow orange red`
  - 단어 사이에 공백이 여러 개 있다면 한 개만
  - 문자열을 단순히 뒤집는 건 `StringBuilder`로 만들고 `reverse()`를 이용해 간단히 뒤집을 수 있지만 여기서는 그것도 다 구현
    - `StringBuilder sb=new StringBuilder(s);`
    - `sb.reverse().toString()`  

  1. 여러 개인 공백 제거  
  2. 문자열 전체 뒤집기  
  3. 단어 별로 다시 뒤집기 (원래대로)  
    
```java
public static String solution(String s) {
    s = removeSpaces(s);

    char[] cArr = s.toCharArray();
    reverseString(cArr, 0, cArr.length - 1);

    reverseWords(cArr, s.length());

    return new String(cArr);
}

public static String removeSpaces(String s) {
    int p1 = 0;
    int p2 = 0;

    char[] cArr = s.toCharArray();
    int length = s.length();

    while (p2 < length) {
        //단어 앞 공백 skip
        while (p2 < length && cArr[p2] == ' ') {
            p2++;
        }

        while (p2 < length && cArr[p2] != ' ') {
            cArr[p1++] = cArr[p2++];
        }

        //단어 뒤 공백 skip
        while (p2 < length && cArr[p2] == ' ') {
            p2++;
        }

        //단어 앞 뒤 공백을 모두 삭제하고 넣었으므로 단어 사이의 공백 추가
        if (p2 < length) {
            cArr[p1++] = ' ';
        }
    }
    return new String(cArr).substring(0, p1);
}

public static void reverseString(char[] cArr, int i, int j) {
    while (i < j) {
        char tmp = cArr[i];
        cArr[i++] = cArr[j];
        cArr[j--] = tmp;
    }
}

public static void reverseWords(char[] cArr, int length) {

    int p1 = 0;
    int p2 = 0;

    while (p1 < length) {
        p1 = p2;
        while (p1 < length && cArr[p1] == ' ') {
            p1++;
        }
        p2 = p1;
        while (p2 < length && cArr[p2] != ' ') {
            p2++;
        }

        reverseString(cArr, p1, p2 - 1);
    }
}
```
- 주어진 배열에서 세 숫자의 합이 0이 되는 숫자 조합 모두 구하기 (중복 X)
  - ex) `{-1, 0, 1, 2, -1, -4}` → `[[-1, -1, 2], [-1, 0, 1]]`

  1. 주어진 배열 오름차순 정렬
  2. for 문으로 `0`부터 `배열.length-2`까지 돌면서 (`p1=i+1`, `p2=배열length-1`)

    - `배열[i]+배열[p1]+배열[p2]` 값이
      - 0이면 리스트에 추가
      - 0보다 작으면 `p1++;`
      - 0보다 크면 `p2--;`

  - 중복 값을 거르기 위해 인덱스 이동할 때 전의 값과 같은지 확인하고 같으면 다를 때까지 이동

```java
public static ArrayList<ArrayList<Integer>> solution(int[] nums) {

    Arrays.sort(nums);
    ArrayList<ArrayList<Integer>> result = new ArrayList<>();

    for (int i = 0; i < nums.length - 2; i++) {
        if (i == 0 || nums[i] != nums[i - 1]) {//중복 피하기 위해
            int p1 = i + 1;
            int p2 = nums.length - 1;

            while (p1 < p2) {
                if (nums[i] + nums[p1] + nums[p2] == 0) {
                    result.add(new ArrayList<>(Arrays.asList(nums[i], nums[p1], nums[p2])));

                    //중복 피하기 위해
                    while (p1 < p2 && nums[p1] == nums[p1 + 1]) {
                        p1++;
                    }
                    while (p1 < p2 && nums[p2] == nums[p2 - 1]) {
                        p2--;
                    }
                    p1++;
                    p2--;
                } else if (nums[i] + nums[p1] + nums[p2] < 0) {
                    p1++;
                } else {
                    p2--;
                }
            }
        }
    }
    return result;
}
```



## 🔔 Tomorrow

- ec2 RDS 설정
- 투 포인터, 그리디 문제

# 18 Wed
## 🌈AWS

- 참고 : [스프링 부트와 AWS로 혼자 구현하는 웹 서비스](https://book.naver.com/bookdb/book_detail.nhn?bid=15871738){:target="_blank"}

###  EC2
#### 서버 생성 시 해야 할 설정들

- Java 11 설치

```bash
$ sudo apt install openjdk-11-jdk
```
```bash
$ java -version
```

![image](https://user-images.githubusercontent.com/90626691/168938277-3c7fa3cb-d03f-4aba-b06f-71f3a4afe3f9.png)

- 타임존 변경
  - 참고 : [우분투에서 타임존 변경하기](https://twpower.github.io/95-set-ubuntu-timezone){:target="_blank"}
  
```bash
$ sudo ln -sf /usr/share/zoneinfo/Asia/Seoul /etc/localtime
```

![image](https://user-images.githubusercontent.com/90626691/168939060-66a9dab8-73a2-4ae3-9a33-fa97ebb688a7.png)



- 호스트네임 변경
  - 참고 : [Ubuntu / hostname 확인하는 방법, 변경하는 방법](https://www.manualfactory.net/13421){:target="_blank"}
```bash
$ hostnamectl set-hostname zb-eongiin
```
  - 그런데 나는 이게 안되고 무슨 패스워드를 입력하라 해서 무슨 패스워드를 입력하라는 거지..? 하고 두 번째 방법으로 했다
  - `/etc/hostname` 열고 내용을 `zb-eongiin`으로 바꿔줬다
  - 재접속 (처음에 접속이 안되다가 시간이 좀 지난 후 다시 접속해 보니 됐다)  
  ![image](https://user-images.githubusercontent.com/90626691/168940766-d23521d8-16ab-4b13-a691-ba6f422e5850.png)



###  RDS
#### RDS 인스턴스 생성


![image](https://user-images.githubusercontent.com/90626691/168935137-efae48d5-e841-4740-8bad-46c6bb3a5359.png)

![image](https://user-images.githubusercontent.com/90626691/168935414-8144c2e9-f0a3-4bf3-8f0a-799f76e572b2.png)

![image](https://user-images.githubusercontent.com/90626691/168935071-cf92399a-3556-4af4-88c3-8c7385eda08d.png)

![image](https://user-images.githubusercontent.com/90626691/168935528-e5c44027-dab5-4a0f-802b-9bb614984479.png)

![image](https://user-images.githubusercontent.com/90626691/168935624-abc6fe74-69d0-4cc0-ac72-b5a1d4ac85f6.png)

![image](https://user-images.githubusercontent.com/90626691/168935711-f0f9db07-78b5-4808-bc73-0ed232f327a1.png)

![image](https://user-images.githubusercontent.com/90626691/168936083-e0d771a4-b5e5-43c9-aa1b-befdfac3bcf4.png)

#### RDS 운영환경에 맞는 파라미터 설정하기
타임존, Character Set, Max Connection 설정을 해줘야 한다

- 파라미터 그룹 생성

![image](https://user-images.githubusercontent.com/90626691/168955764-942cb587-dc9c-4df9-9813-1787d065a0d1.png)

- 파라미터 편집
  - time_zone

  ![image](https://user-images.githubusercontent.com/90626691/168956095-07df8768-4f7e-401f-8a3f-11ca6f37a09e.png)
  - Character Set
    - 아래 6개 항목을 모두 `utf8mb4`로 바꾼다 (utf8과 utf8mb4의 차이는 이모지 저장 가능 여부)
      - character_set_client
      - character_set_conection
      - character_set_database
      - character_set_filesystem
      - character_set_results
      - character_set_server

      ![image](https://user-images.githubusercontent.com/90626691/168957582-b3f779e1-e61b-43ab-ab1b-0e200eb9a415.png)
    - 아래 2개 항목은 `utf8mb4_general_ci`로 바꾼다
      - collation_connection
      - collation_server
  - Max Connection
    - 나는 `LEAST({DBInstanceClassMemory/25165760},12000)`로 되어 있었는데 좀 더 넉넉하게 150 정도로 지정하겠다

    ![image](https://user-images.githubusercontent.com/90626691/168960204-c0ad469e-09ba-49ee-961d-ae73271953d2.png)

- 생성한 파라미터 그룹을 데이터베이스에 연결
  - 데이터베이스 인스턴스(`zb-webservice`)  수정
  
  ![image](https://user-images.githubusercontent.com/90626691/168961029-13ee4533-3bd3-41b6-ba89-9133fb948e86.png)

  ![image](https://user-images.githubusercontent.com/90626691/168961196-6c705a70-c99c-4425-95f6-d673be059a60.png)

  - 간혹 파라미터 그룹이 제대로 반영되지 않을 때가 있으니 재부팅 진행

#### 내 PC에서  RDS 접속하기

- EC2에 사용된 보안 그룹의 그룹 ID 복사 (`sg-04792ae2f390886c3`)

![image](https://user-images.githubusercontent.com/90626691/168967665-179cb461-d699-4053-b749-26a14ceee517.png)

- 복사된 보안 그룹 ID와 내 IP를 RDS 보안 그룹의 인바운드로 추가  
  (RDS 보안 그룹이 default로 되어있길래 하나 만들고 추가했다, 인스턴스에 연결도 잊지 말기)
  - 보안 그룹 이름 : `zb-webservice-rds`
  - 설명 : `rds`

![image](https://user-images.githubusercontent.com/90626691/168968982-d5999b21-7176-41fd-a174-1c0a6dc09a03.png)

![image](https://user-images.githubusercontent.com/90626691/168969365-1418d681-55c2-4d7a-b7bf-89113ec90fac.png)


##### IntelliJ에 Database 플러그인 설치

- `ctrl+shift+a` Action 검색창에서 `plugins`검색
- `database` 플러그인 검색하고 설치

![image](https://user-images.githubusercontent.com/90626691/168970687-866ac7cc-b976-4ffe-ba27-3e9fdf797c1c.png)

- 재시작하고 Action 검색으로 `Database Browser`실행

![image](https://user-images.githubusercontent.com/90626691/168971322-f2b6a1bd-4074-4a75-aa45-2c013974e852.png)

- `+` 누르고 `MySQL` 눌러서 접속 정보를 등록한다
  - ~~MariaDB는 MySQL 기반이므로 MySQL을 사용하면 된다~~
    - 지금은 RDS를 MariaDB로 생성하고 IntelliJ에서 MySQL로 접속하려고 하면 오류가 있는듯하다

![image](https://user-images.githubusercontent.com/90626691/168971749-aaa2c95a-d09d-4b92-a6b2-53d71161f1e7.png)

![image](https://user-images.githubusercontent.com/90626691/168974669-a9da4879-dd8b-43c8-b7d8-71c69c971acf.png)

  - 연결이 안 된다!
    - 구글링 해보니.. 비슷한 문제를 겪고 있는 사람이 많았다
    - ultimate 버전을 쓴다면 그냥 우측의 `database`을 쓰면 된다
    - community 버전은 그게 없다.. → [해결방법](https://github.com/jojoldu/freelec-springboot2-webservice/issues/757#issuecomment-1018212985){:target="_blank"}
      - MySQL이 아닌 custom을 클릭하고 MariaDB 드라이버를 설치해서 연결하는 방식
    - 나는 그냥 DBeaver로 연결하겠다

##### DBeaver

![image](https://user-images.githubusercontent.com/90626691/168974142-083b44d5-c417-4dab-aec3-0526a4940800.png)

- host에는 RDS 엔드 포인트를 넣어주면 된다

##### 바꾼 설정 확인

- character_set, collation 설정 확인

```sql
show variables like 'c%';
```

![image](https://user-images.githubusercontent.com/90626691/168982393-cf0e6f45-40e1-4e87-9fe7-ba47085110b9.png)

- 몇 가지 값이 잘못 들어가 있어서 직접 변경

```sql
alter database zbdb
character set ='utf8mb4'
collate ='utf8mb4_general_ci';
```

![image](https://user-images.githubusercontent.com/90626691/168983426-d93873c6-b679-4b6c-ac2d-c676d6202fd5.png)

- 타임존 확인

```sql
select @@time_zone, now();
```

![image](https://user-images.githubusercontent.com/90626691/168985146-048dd3b6-e071-4873-84d5-fec705b8c713.png)

- 한글명이 잘 들어가는지 간단한 테이블 생성과 데이터 입력

```sql
create table test(
	id bigint(2) not null auto_increment,
	content varchar(255) default null,
	primary key(id)
) engine = innodb;

insert into test(content) values('테스트');
select * from test;
```

![image](https://user-images.githubusercontent.com/90626691/168984566-f2356229-1b4d-452f-8d03-f261d8be0c5d.png)

#### EC2에서 RDS 접근

- 접근 테스트를 위해 ec2에 ssh 접속 후 mysql 설치

```bash
$ sudo apt-get install mysql-server
```

```bash
$ mysql -u 계정 -p -h host 주소(엔드 포인트)
```

![image](https://user-images.githubusercontent.com/90626691/168986615-f004c0d8-7c8c-4b1e-b0eb-436bafa81fd4.png)

성공적으로 접속 완료!


## 🔔 Tomorrow

- 투 포인터 문제
- Coding Test

# 19 Thu

## 🌻 Coding Test

- 투 포인터에 대해 더 공부했어야 했는데.. 아직 헷갈리는 많은 상태로 하다 보니 처음부터 실수도 잦고, 많이 헤맸다
- 1번은 기본적인 문제였고, 2번은 문제 이해만 잘하면 쉬운 문제였고, 3번을 풀다가 내가 헷갈리는 부분 때문에 굉장히 오랜 시간 헤맸다
- 적당히 풀다가 넘어갔어야 했는데 결국 50분 정도만 남겨둔 채 4번으로 넘어갔고 4번도 굉장히 쉽게 풀 수 있는 방법이 있었는데 실수를 계속하다 보니 끝나기 5분 전에 겨우 통과했다
- 그러고 나서 끝나기 직전에 5번을 읽었는데...................  전에 수업 시간에 풀었던 문제랑 완전히 같은 문제였다
  - 원래 문제의 풀이 대로 하면 더 수정을 해줬어야 했지만, 내가 푼 방식은 이 문제에 그대로 적용할 수 있었다
  - 3번을 빨리 넘기고 4,5번을 차분히 풀었어야 했는데.. 아쉬움이 많이 남는다..
- 아쉬움이 크지만 그래도 내가 투 포인터에 대해 공부가 부족했던 탓이니 어쩔 수 없다
- 이번 시험범위가 투 포인터, 그리디였는데 문제 의도대로 안 푼 문제도 있긴 하다  
- 토요일에 차분히 문제 의도대로 풀어봐야겠다

## 🔔 Tomorrow

- 리눅스 퀴즈
- DB 수업

# 20 Fri
## 🔔 Tomorrow

- 코테 문제 확인

# 21 Sat

어제오늘 여러 일이 있어서 정신이 없었다..  
오늘은 코테 문제 확인만 간단히 했다  
좀 더 정리가 필요한데 내일~화요일 중에 시간이 날 때 하겠다  
내일부터 다시 열심히 공부해야겠다

## 🔔 Tomorrow

- 데이터베이스 개념 및 실습
- 자바 프로그래밍 기본 개념 및 실습
- ERD를 통한 데이터베이스 설계
- 웹을 통한 데이터베이스 처리 프로그래밍 실습

# 22 Sun

오늘도 컨디션이 좋지 않아 쉬었다..

## 🔔 Tomorrow

- 데이터베이스 개념 및 실습
- 자바 프로그래밍 기본 개념 및 실습
- ERD를 통한 데이터베이스 설계
- 웹을 통한 데이터베이스 처리 프로그래밍 실습