---
layout: post
title: August 1st 2022
subtitle: 2022.08.01~08.07
# description: >
#   2022.08. 첫째 주 알게 된 내용
date: '2022-08-01 0:0:0'
categories:
  - til
tags: [code-write,db,java,spring,pj05]
related_posts:
  - 
sitemap: false
published: true
---
# [ 22.08.1st ] : Spring, Project

* toc 
{:toc}

# 01 Mon
## ✏️ 좋은 코드 작성법
### 디자인 패턴
#### Proxy

<b>해결하려는 문제</b>

객체에 접근을 제어하고 싶을 때  
1.  객체를 실제 사용하기 전까지 초기화를 미루고 싶을 때 (Lazy init)
2. 접근 권한 체크

![image](https://user-images.githubusercontent.com/90626691/182058786-f314ec9b-024e-4b66-9418-2d837135e7b1.png){: .align-center}*[출처](https://ko.wikipedia.org/wiki/%ED%94%84%EB%A1%9D%EC%8B%9C_%ED%8C%A8%ED%84%B4){: .img-cation target="_blank"}*


[코드](https://github.com/eongiin/study/commit/db4ccdb7edfbd27755789f9731ef25b458224675){:target="_blank"}

#### Strategy

<b>해결하려는 문제</b>

- 클라이언트에 독립적으로 문제 해결 전략을 바꾸고 싶을 때
- 다양한 문제해결 방법을 제공해야 할 때

![image](https://user-images.githubusercontent.com/90626691/182061107-87a1be15-063a-4642-a751-3002c92138d6.png){: .align-center}*[출처](https://ko.wikipedia.org/wiki/%EC%A0%84%EB%9E%B5_%ED%8C%A8%ED%84%B4){: .img-cation target="_blank"}*

[코드](https://github.com/eongiin/study/commit/a4c514a1d8b0b6b20a2cf00ba0413ee6974065dd){:target="_blank"}



## 💰 배당금 프로젝트

### 프로젝트 생성

![image](https://user-images.githubusercontent.com/90626691/182079171-85db4cdf-a47d-4205-bc7a-b88cc47b4bb4.png)

![image](https://user-images.githubusercontent.com/90626691/182078237-5d82c959-fdbb-453d-bb8f-14955f90aeaf.png)

### 스크래핑

- [야후 파이낸스](https://finance.yahoo.com/>){:target="_blank"}에서 정보 가져올 예정
- robots.txt 문서 확인
  - <https://www.yahoo.com/robots.txt>{:target="_blank"}
  - `https://finance.yahoo.com/quote`에서 가져올 건데 그 경로가 disallow 되어 있지 않음 확인
- [jsoup API docs](https://jsoup.org/apidocs/){:target="_blank"}

- 코카콜라의 배당금 정보 가져와서 테스트

![image](https://user-images.githubusercontent.com/90626691/182084447-39d06abe-b9fc-41eb-b9b3-d7567fe066a0.png)

```java
Connection connect = Jsoup.connect("https://finance.yahoo.com/quote/COKE/history?period1=99100800&period2=1659312000&interval=1mo&filter=history&frequency=1mo&includeAdjustedClose=true");
  try {
      Document document = connect.get();

      Elements eles = document.getElementsByAttributeValue("data-test", "historical-prices");
      Element table = eles.get(0);
      Element tbody = table.children().get(1);

      for (Element e : tbody.children()) {
          String txt = e.text();
          if (!txt.endsWith("Dividend")) {
              continue;
          }

          String[] splits = txt.split(" ");
          String month = splits[0];
          int day = Integer.valueOf(splits[1].replace(",", ""));
          int year = Integer.valueOf(splits[2]);
          String dividend = splits[3];
          
      }
  } catch (IOException e) {
      throw new RuntimeException(e);
  }
```
### 서비스 설계
[코드](https://github.com/eongiin/Dividends/commit/22f764f7f22e773a539cc22889c42cca8a7ffb54){:target="_blank"}


## 🔔 Tomorrow
- [x] 클린 코드
- [x] 좋은 코드 작성법
- [x] 배당금 프로젝트
- [ ] 스프링 MVC

<!-- # 02 Tue -->
<!-- ## ✏️ 좋은 코드 작성법
### 디자인 패턴
#### Template Method
<b>해결하려는 문제</b>

- 문제해결의 큰 흐름은 유지하고, 단계별 처리는 위임하고자 할 때
- 구체적인 처리 방법은 하위 클래스로 위임하고자 할 때

![image](https://user-images.githubusercontent.com/90626691/182272155-a4c93236-89cf-41d6-b034-748f8e8b704f.png){: .align-center}*[출처](https://ko.m.wikipedia.org/wiki/%ED%8C%8C%EC%9D%BC:W3sDesign_Template_Method_Design_Pattern_UML.jpg){: .img-cation target="_blank"}* -->


# 07 Sun

## 💰 배당금 프로젝트

### 배당금 중복 저장 방지


- Unique Key
  - 중복 데이터 저장을 방지하는 제약조건
  - 단일 컬럼 뿐 아니라 복합 컬럼을 지정할 수도 있음

DividendEntity

```java
@Table(
        uniqueConstraints = {
                @UniqueConstraint(
                        columnNames = {"companyId", "date"}
                )
        }
)
```
이렇게 설정해 놓으면 중복된 유니크 키를 가진 데이터를 넣을 때 Exception 발생  

- 중복 처리 하기
  - 데이터가 존재하는지 확인하고 존재 하지 않는 경우에만 삽입
    - companyId, date 정보로 확인
    - unique key 설정을 해두었기 때문에 좀 더 빠르게 조회 가능
  - 중복 신경 쓰지 않고 넣은 후 Exception 발생하면 핸들링
  - 데이터를 넣을 때 기본 쿼리문 대신 ignore나 on duplicate key update
    - `INSERT IGNORE INTO [TABLE] (COLUMN1, COLUMN2) VALUES (VALUE1, VALUE2)`
      - 중복키를 가진 데이터가 들어왔을 때 해당 레코드를 아예 무시
    - `INSERT INTO [TABLE] (COLUMN1, COLUMN2) VALUES (VALUE1, VALUE2) ON DUPLICATE KEY UPDATE ...`
      - 중복된 키의 레코드가 들어왔을 때 중복된 키의 컬럼값 중에서 UPDATE 키워드 뒤에 정의된 컬럼들의 값을 업데이트

### 스케줄러

Scheduler

-  fixedDelay
  - 이전 수행이 종료된 시점을 기준으로 일정 시간이 경과했을 때 다음 수행 시작
- fixedRate
  - 이전 수행이 시작된 지점을 기준으로 일정 시간이 경과햇을 때 다음 수행 시작
- cron
  - 초/ 분/ 시/ 일/ 월/ 요일/ 년도(생략가능)
    - 0 5 * * * * (매시 5분마다)
    - 0 0/10 * * * * (10분에 한번씩)
    - 0 5 1 ? 7 MON-WEB (매년 7월 월-수 1시 5분)
  - 월 1~12 or JAN~DEC
  - 요일 0~6 or SUN~SAT
  - 연도 1970~2099
  - `*` : 모든 수
  - `?` : 조건 없음 - 날짜와 요일에만 사용
  - `-` : 범위(기간) 지정
  - `,` : 특정 여러 시간 지정
  - `/` : 시작 시간과 반복 간격
  - `L` : 지정할 수 있는 범위의 마지막 값 - 날짜와 요일에만 사용

#### 배당금 정보 주기적으로 가져오기

1. 저장된 회사 목록을 조회
2. 회사마다 배당금 정보를 새로 스크래핑
3. 스크래핑한 배당금 정보 중 데이터베이스에 없는 값은 저장

```java
@Scheduled(cron = "0 0 0 * * *")
public void yahooFinanceScheduling() {

//        log.info("scraping scheduler is started");
    // 저장된 회사 목록 조회
    List<CompanyEntity> companies = this.companyRepository.findAll();

    // 회사마다 배당금 정보를 새로 스크래핑
    for (CompanyEntity company : companies) {
        log.info("scraping scheduler is started -> " + company.getName());
        ScrapedResult scrapedResult = this.yahooFinanceScraper.scrap(Company.builder()
                .name(company.getName())
                .ticker(company.getTicker())
                .build());

        // 스크래핑한 배당금 정보 중 데이터베이스에 없는 값은 저장
        scrapedResult.getDividends().stream()
                .map(e -> new DividendEntity(company.getId(), e))
                .forEach(e -> {
                    boolean exists = this.dividendRepository.existsByCompanyIdAndDate(e.getCompanyId(), e.getDate());
                    if (!exists) {
                        this.dividendRepository.save(e);
                    }
                });
        // 연속적으로 스크래핑 대상 사이트 서버에 요청을 날리지 않도록 일시 정지
        try {
            Thread.sleep(3000);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        }
    }
}
```

변경될 수 있는 값은 Config로 관리

application.yml
```yml
scheduler:
  scrap:
    yahoo: "0 0 0 * * *"
```

```java
@Scheduled(cron = "${scheduler.scrap.yahoo}")
```

#### Thread Pool

scheduler로 실행되어야 하는 기능이 여러 개면 Thread Pool 고려해야 함

Thread Pool
- 여러 개의 스레드를 유지/관리

```java
@Configuration
public class SchedulerConfig implements SchedulingConfigurer {
    @Override
    public void configureTasks(ScheduledTaskRegistrar taskRegistrar) {
        ThreadPoolTaskScheduler threadPool = new ThreadPoolTaskScheduler();
        
        int n = Runtime.getRuntime().availableProcessors();//코어 개수
        threadPool.setPoolSize(n);
        threadPool.initialize();

        taskRegistrar.setTaskScheduler(threadPool);
    }
}
```

### Redis

- key-value
- 인메모리 데이터 스토어
- 다양한 형태의 데이터 타입 지원
- 주로 캐시 서버 용도로 많이 사용

#### 설치

<https://redis.io/docs/getting-started/>{:target="_blank"}  
나는 윈도우를 쓰고 있어서 해당 페이지에 나와있는 대로 쭉 설치했다    
전에 한 번 WSL2 설치한적이 있어서 아래 명령들만 실행해주면 됐다  


```
curl -fsSL https://packages.redis.io/gpg | sudo gpg --dearmor -o /usr/share/keyrings/redis-archive-keyring.gpg

echo "deb [signed-by=/usr/share/keyrings/redis-archive-keyring.gpg] https://packages.redis.io/deb $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/redis.list

sudo apt-get update
sudo apt-get install redis
```

![image](https://user-images.githubusercontent.com/90626691/183278055-3f2029e2-65a9-4918-a9c8-0e1bc6c9af19.png)

잘 떠있는지 새 창을 열고 확인 
- `ps aux | grep -i redis`

![image](https://user-images.githubusercontent.com/90626691/183278160-1d60006d-1eef-4e48-ae66-562bbea84663.png)

#### Redis 명령어
```
$> set myKey myValue
$> OK
```

```
$> get myKey
$> myValue
```

```
$> del myKey
$> myValue
```

```
$> get myKey
$> (nil)
```

```
$> keys *
```

```
$> flushall
```

[더 많은 명령어 참고](http://redisgate.kr/redis/introduction/redis_intro.php){:target="_blank"}

![image](https://user-images.githubusercontent.com/90626691/183278407-5eb00863-a33b-4882-9795-3d99412123dd.png)

- 정보 확인 `redis-cli info`  
- 종료 `shutdown`
- 설정 파일 보기 `sudo vi /etc/redis/redis.conf`
- 비밀번호 설정 
  - ![image](https://user-images.githubusercontent.com/90626691/183279843-8277eef3-0133-403b-b175-54ccd32814a2.png)
  - `#`지우고 foobared에 설정할 비밀번호 값 넣기
- 포트 설정
  - 마찬가지로 port 검색해서 6379(기본값)가 아닌 다른 값 설정해줄 수 있음
- 설정을 바꿨으면 `redis-server /etc/redis/redis.conf`로 설정 읽게 하기

#### Spring에서 Redis 접근

- 의존성 추가
```
implementation 'org.springframework.boot:spring-boot-starter-data-redis'
```

- application.yml

```yml
spring:
  ...
  redis:
    host: localhost
    port: 6379
```

- SpringBootApplication에 `@EnableCaching` 추가
- CaheConfig 생성

```java
@RequiredArgsConstructor
@Configuration
public class CacheConfig {
    @Value("${spring.redis.host}")
    private String host;

    @Value("${spring.redis.port}")
    private int port;

    @Bean
    public RedisConnectionFactory redisConnectionFactory() {
        RedisStandaloneConfiguration conf = new RedisStandaloneConfiguration();
        conf.setHostName(this.host);
        conf.setPort(this.port);
        return new LettuceConnectionFactory(conf);
    }
}
```
일단 redis와 connection을 맺을 수 있는 ConnectionFactory만 만들어 준 상태  
캐시에 적용시켜 사용하기 위해서 CacheManagerBean 생성  

```java
@Bean
public CacheManager redisCacheManager(RedisConnectionFactory redisConnectionFactory) {
    RedisCacheConfiguration conf = RedisCacheConfiguration.defaultCacheConfig()
            .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(new StringRedisSerializer()))
            .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(new GenericJackson2JsonRedisSerializer()));
    return RedisCacheManager.RedisCacheManagerBuilder.fromConnectionFactory(redisConnectionFactory)
            .cacheDefaults(conf)
            .build();
}
```
Serialization (직렬화)
- 데이터 or 오브젝트와 같은 값들을 바이트 형태로 변환
  - 왜? 바이트 형태로 바꾸면 java 외부에서도 데이터 사용 가능


#### 배당금 조회 캐시 적용하기

- 캐시 적용할 대상에  `@Cacheable` 붙여주기
- 배당금 조회하는 `ScrapedResult getDividendByCompanyName(String companyName) {}`에 붙임
  - `@Cacheable(key = "#companyName", value = "finance")`
- Dividend 클래스에서 LocalDateTime 어떻게 직렬화, 역직렬화 할건지 지정
```java
@JsonSerialize(using = LocalDateTimeSerializer.class)
@JsonDeserialize(using = LocalDateTimeDeserializer.class)
```
- 적용하고 회사명으로 배당금 조회를 해보니 에러 발생
  ```
  com.fasterxml.jackson.databind.exc.InvalidDefinitionException: Cannot construct instance of `com.eongiin.dividends.model.Dividend` (no Creators, like default constructor, exist): cannot deserialize from Object value (no delegate- or property-based Creator)
  ```
  - Dividend 클래스에 `@NoArgsConstructor`, `@AllArgsConstructor` 추가

- 하지만 또 다른 에러 발생
  - `Java 8 date/time type 'java.time.LocalDateTime' not supported by default: add Module "com.fasterxml.jackson.datatype:jackson-datatype-jsr310" to enable handling`
  - Springboot 2.~부터는 jsr310이 자동으로 들어가 있다고 하는데.. 왜 이런 걸까
  - 이 과정에서 다양한 오류를 만나고 다양한 해결 방법을 시도해 봐서 정확히 어떤 순서로 해결한 건지는 잘 모르겠지만.. 결과적으로는 아래와 같이 objectMapper를 만들어주고 해결했다
  ```java
  private ObjectMapper objectMapper() {
      ObjectMapper objectMapper = new ObjectMapper();
      PolymorphicTypeValidator ptv = BasicPolymorphicTypeValidator
              .builder()
              .allowIfSubType(Object.class)
              .build();
      objectMapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
      objectMapper.registerModule(new JavaTimeModule());
      objectMapper.activateDefaultTyping(ptv, ObjectMapper.DefaultTyping.NON_FINAL);
      return objectMapper;
  }
  ```
  - 처음에는 `objectMapper.registerModule(new JavaTimeModule());`이 부분만 수행하고 반환했었는데 그랬더니 `java.lang.ClassCastException: class java.util.LinkedHashMap cannot be cast to class com.eongiin.dividends.model.ScrapedResult (java.util.LinkedHashMap is in module java.base of loader 'bootstrap'; com.eongiin.dividends.model.ScrapedResult is in unnamed module of loader 'app')` 이런 에러가 발생해서 구글링해서.. 어디선가.. 저 ptv 부분을 복붙했더니 해결됐다..
  - 일단 넘어가고 다음에 다시 알아봐야겠다
  - [직접 custom 하기 참고](https://github.com/HomoEfficio/dev-tips/blob/master/Java8-LocalDateTime-Jackson-%EC%A7%81%EB%A0%AC%ED%99%94-%EB%AC%B8%EC%A0%9C.md){:target="_blank"}
  

- 배당금 조회를 한 번 하고(`GET {{host}}/finance/dividend/3M Company`) redis-cli 확인
  - ![image](https://user-images.githubusercontent.com/90626691/183298042-12350dc8-4cf5-4f14-9bd0-fbc826e3c3d7.png)

- 레디스 캐시 주기적으로 삭제
  - 전에 작성했던 주기적으로 배당금 정보 가져오는 함수에 `@CacheEvict(value = "finance", allEntries = true)` 추가
  - 해당 클래스에 `@EnableCaching` 추가
  - TTL(Time To Live) 설정해서 시간이 지나면 자동으로 지우는 것도 가능

## 🔔 Tomorrow
- [ ] 배당금 프로젝트
- [ ] MoVieC