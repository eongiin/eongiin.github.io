---
layout: post
title: January 3rd 2023
subtitle: 2023.01.16~01.22
# description: >
#   2023.01 셋째 주 알게 된 내용
date: '2023-01-16 0:0:0'
categories:
  - til
tags: [java]
related_posts:
  - 
sitemap: false
published: true
---
# [ 23.01.3rd ] : Spring MVC, Book, PS

* toc 
{:toc}

# 18 Wed
## 🌻PS
- [신을 모시는 사당](https://www.acmicpc.net/problem/27210)
  - if 문을 쓰고 싶지 않아서 이차원 배열을 만들어서 풀었는데 사실 이차원 배열까지 필요 없고 그냥 일차원 배열 int[2] 만으로 풀 수 있음
  - sum[0] → 왼쪽, sum[1] → 오른쪽 체크
  - 현재 확인하는 것이 왼쪽이면 sum[0]에 +1 하고 sum[1]은 -1
    - 이때 음수가 되지 않도록 0으로 설정
    - 0이 된다는 것은 현재 위치 기준 그전 위치까지의 합을 더해봤자 더 손해라 다음 위치부터 새로 시작한다는 의미
    - 현재 확인하는 값은 `num` 다른 값은 `num ^ 0 ^ 1`로 접근 가능
      - `num ^ 0 ^ 1` - xor 연산
        -  num이 0이면 1을 반환하고 1이면 0을 반환


  ```java
  int[] sum = new int[2];
  int max = 1;
  StringTokenizer st = new StringTokenizer(br.readLine());
  for (int i = 0; i < n; i++) {
      int num = Integer.parseInt(st.nextToken()) - 1;
      sum[num]++;
      sum[num ^ 0 ^ 1] = Math.max(0, sum[num ^ 0 ^ 1] - 1);
      max = Math.max(max, sum[num]);
  }
  System.out.print(max);
  ```

- [오아시스 재결합](https://eongiin.github.io/problem-solving/problem/2023-01-11-pb075/){:target="_blank"}
  - 며칠 전에 푼 문젠데 오늘 다시 정리해 봤다
  - 근데 처음부터 저렇게 생각하고 풀지는 못할 것 같다
  - 나중에 다시 풀어봐야겠다


## 📖 Book
- 시스템은 역할과 책임을 수행하는 객체로 분할되고 시스템의 기능은 객체 간의 연쇄적인 요청과 응답의 흐름으로 구성된 협력으로 구현됨

객체의 역할
- 여러 객체가 동일한 역할을 수행할 수 있다
- 역할은 대체 가능성을 의미한다
- 각 객체는 책임을 수행하는 방법을 자율적으로 선택할 수 있다
- 하나의 객체가 동시에 여러 역할을 수행할 수 있다

객체지향의 개념 간단 정리
- 객체지향이란 시스템을 상호작용하는 <b>자율적인 객체들의 공동체</b>로 바라보고 객체를 이용해 시스템을 분할하는 방법
- 자율적인 객체란 <b>상태</b>와 <b>행위</b>를 함께 지니며 스스로 자기 자신을 책임지는 객체를 의미
- 객체는 시스템의 행위를 구현하기 위해 다른 객체와 <b>협력</b>함  
각 객체는 협력 내에서 정해진 <b>역할</b>을 수행하며 역할은 관련된 <b>책임</b>의 집합
- 객체는 다른 객체와 협력하기 위해 <b>메시지</b>를 전송하고, 메시지를 수신한 객체는 메시지를 처리하는 데 적합한 <b>메서드</b>를 자율적으로 선택

[객체지향의 사실과 오해](http://www.yes24.com/Product/Goods/18249021){:target="_blank"}

## 🔔 Tomorrow
- PS
- Spring MVC
- JPA
- 독서

# 19 Thu

## 🌻PS
- [미팅](https://eongiin.github.io/problem-solving/problem/2023-01-19-pb076/){:target="_blank"}
  - DP 문제 
  - 그동안 풀어봤던 DP 문제랑 비슷한 느낌
  - 자잘한 실수를 여러 번 했다(long 타입으로 배열 만들기, 가장 바깥 부분 따로 계산해 줄 때 단순 계산이 아닌 거기도 max 값 구해야 하는 것, 아예 선택 안 한 경우 고려)
- 내일부터는 다시 시간 재고 2문제 풀기


## ✏️ Spring MVC

[스프링 MVC 1편 ](https://www.inflearn.com/course/%EC%8A%A4%ED%94%84%EB%A7%81-mvc-1/dashboard){:target="_blank"}

### Model 추가 v3
- 서블릿 종속성 제거
  - 요청 파라미터 정보는 Java의 Map으로 대신 넘기기
    - `Map<String, String> paramMap = createParamMap(request);`
    - createParamMap 메서드에서 request에 담겨있는 파리미터 정보들을 Map에 하나씩 담아서 반환
  - request 객체 대신 별도의 Model 객체를 만들어서 반환하기(`modelView` 로 만들 예정)
    - `ModelView mv = controller.process(paramMap);`  
→ 컨트롤러가 서블릿 기술을 몰라도 동작 가능  
→ 구현 코드도 단순해지고 테스트 코드 작성이 쉬움
- 뷰 이름 중복 제거
  - 컨트롤러는 뷰의 논리 이름을 반환(`modelView`애 논리 이름과 모델로 사용할  map이 담겨있음) 하고 물리 위치의 이름은 프론트 컨트롤러에서 처리하도록 함
    - viewResolver 메서드 만들어서 논리 뷰 이름을 받으면 실제 물리 뷰 경로로 변경 후  MyView 객체에 담아서 반환
    - `MyView view = viewResolver(viewName)`  
    - viewResolver → `return new MyView("/WEB-INF/views/" + viewName + ".jsp");`  
    →  나중에 변경돼도 프론트 컨트롤러(viewResolver 부분)만 고치면 됨
- `view.render(mv.getModel(), request, response)`
  - 뷰 객체를 통해 HTML 화면 렌더링
  - JSP는 `request.getAttribute()`로 데이터를 조회하기 때문에 모델의 데이터를 꺼내서 `request.setAttribute()` 작업 필요
  - JSP로 포워드 해서 JSP 렌더링

[코드](https://github.com/eongiin/spring04-mvc1/commit/844847a71bf6b5255ea17e00eaa73ff34f6dc3f3){:target="_blank"}

### 단순하고 실용적인 컨트롤러 v4

- v3에서 ModelView를 반환하지 않고 ViewName만 반환하는 것으로 변경
- FrontController에서 Model 객체를 생성하여 controller에 파라미터 정보와 함께 전달
  - `String process(Map<String, String> paramMap, Map<String, Object> model)`
  - controller에서 모델 객체 매번 안 만들어줘도 됨
  - ViewName만 반환

[코드](https://github.com/eongiin/spring04-mvc1/commit/ff06bdcb00419340fc07064da92a92683bf8f02d){:target="_blank"}

## 🔔 Tomorrow
- PS
- Spring MVC
- JPA
- 독서

# 20 Fri

## 📖 Book

[객체지향의 사실과 오해](http://www.yes24.com/Product/Goods/18249021){:target="_blank"}

- 앨리스는 상태를 가지며 상태는 변경 가능하다
- 앨리스의 상태를 변경시키는 것은 앨리스의 행동이다
  - 행동의 결과는 상태에 의존적이며 상태를 이용해 서술할 수 있다  
  → 앨리스의 상태를 결정하는 것은 행동이지만 행동의 결과를 결정하는 것은 상태다
  - 행동의 순서가 결과에 영향을 미친다
- 앨리스는 어떤 상태에 있더라도 유일하게 식별 가능하다

객체
- 식별 가능한 개체 또는 사물
- 자동차처럼 만질 수 있는 구체적인 사물일 수도 있고 시간처럼 추상적인 개념일 수도 있음
- 구별 가능한 식별자, 특징적인 행동, 변경 가능한 상태를 가짐
- 소프트웨어 안에서 객체는 저장된 상태와 실행 가능한 코드를 통해 구현

상태 
- 과거에 발생한 행동의 이력을 통해 현재 발생한 행동의 결과를 판단하는 방식은 복잡하고 번거로우며 이해하기 어려움
- 따라서 행동의 과정과 결과를 단순하게 기술하기 위해 상태라는 개념을 고안
- 상태를 이용하면 과거에 얽매이지 않고 현재를 기반으로 객체의 행동 방식을 이해할 수 있음

상태와 프로퍼티
- 프로퍼티
  - 객체의 상태를 구성하는 모든 특징을 통틀어 객체의 프로퍼티(property)라고 함
    - 일반적으로 프로퍼티는 변경되지 않고 고정되기 때문에 '정적'
    - 프로퍼티 값(property value)은 시간이 흐름에 따라 변경되기 때문에 '동적'
  - 링크 - 객체와 객체 사이의 의미 있는 연결
    - 링크가 존재해야만 객체 사이에 요청(메시지)을 보내고 받을 수 있음
    - 링크는 객체가 다른 객체를 참조할 수 있다는 것을 의미
      - 일반적으로 한 객체가 다른 객체의 식별자를 알고 있음
  - 속성 - 객체를 구성하는 단순한 값
- 객체의 프로퍼티는 단순한 값인 속성과 다른 객체를 가리키는 링크의 두 가지 종류의 조합으로 표현할 수 있음

객체의 상태 정리
- 상태는 특정 시점에 객체가 가지고 있는 정보의 집합으로 구조적 특징을 표현
- 객체에 존재하는 정적인 프로퍼티와 동적인 프로퍼티 값으로 구성
- 객체의 프로퍼티는 단순한 값과 다른 객체를 참조하는 링크로 구분할 수 있음

행동
- 객체의 행동은 상태에 영향을 받음
- 객체의 행동은 상태를 변경시킴
- 행동이란 외부의 요청 또는 수신된 메시지에 응답하기 위해 동작하고 반응하는 활동
- 행동의 결과로 객체는 자신의 상태를 변경하거나 다른 객체에게 메시지를 전달할 수 있음
- 객체는 행동을 통해 다른 객체와의 협력에 참여하므로 행동은 외부에 가시적이어야 함

식별자
- 객체를 서로 구별할 수 있는 특정한 프로퍼티
- 값과 객체의 가장 큰 차이점은 값은 식별자를 가지지 않지만 객체는 식별자를 가진다는 점
- 값
  - 값(value)은 숫자, 문자열, 날짜, 시간, 금액 등과 같이 변하지 않는 양을 모델링
  - 흔히 값의 상태는 변하지 않기 때문에 불변 상태(immutable state)를 가진다고 말함
  - 상태를 이용해 두 값이 같은지 판단 → 동등성(equality)
  - 값의 상태는 결코 변하지 않기 때문에 어떤 시점에 동일한 타입의 두 값이 같다면 언제까지라도 두 값은 동등한 상태를 유지할 것
  - 값은 오직 상태만을 이용해 동등성을 판단하기 때문에 인스턴스를 구별하기 위한 별도의 식별자를 필요로 하지 않음
- 객체
  - 객체는 시간에 따라 변경되는 상태를 포함하며 행동을 통해 상태를 변경 → 가변 상태(mutable state)
  - 타입이 같은 두 객체의 상태가 완전히 똑같더라도 두 객체는 독립적인 별개의 객체로 다뤄져야 함
  - 두 객체의 상태가 다르더라도 식별자가 같다면 두 객체를 같은 객체로 판단할 수 있음
  - 식별자를 기반으로 같은지를 판단 → 동일성(identical)
  - 상태가 가변적인 두 객체의 동일성을 판단하기 위해서는 상태 변경에 독립적인 별도의 식별자를 이용할 수밖에 없음

행동이 상태를 결정한다
- 객체의 행동은 객체가 협력에 참여하는 유일한 방법
- 객체가 적합한지를 결정하는 것은 그 객체의 상태가 아닌 행동
- 객체지향 설계는 애플리케이션에 필요한 협력을 생각하고 협력에 참여하는 데 필요한 행동을 생각한 후 행동을 수행할 객체를 선택하는 방식으로 수행
- 행동을 결정한 후에야 행동에 필요한 정보가 무엇인지 고려 → 상태 결정

<!-- ## 21 Sat

## 📖 Book

[객체지향의 사실과 오해](http://www.yes24.com/Product/Goods/18249021){:target="_blank"}

### 타입과 추상화

> 일단 컴퓨터를 조작하는 것이 추상화를 구축하고, 조작하고, 추론하는 것에 관한 모든 것이라는 것을 깨닫고 나면 (훌륭한) 컴퓨터 프로그램을 작성하기 위한 중요한 전제 조건은 추상화를 정확하게 다루는 능력이라는 것이 명확해진다  
<div class="source">- 키스 데블린(Keith Devlin)[Devlin 2003]</div> -->

 
